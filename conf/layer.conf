# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend \
            ${LAYERDIR}/meta-common/recipes-*/*/*.bbappend \
            ${LAYERDIR}/meta-restricted/recipes-*/*/*.bbappend \
	    "

BBFILE_COLLECTIONS += "meta-ami"
BBFILE_PATTERN_meta-ami = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-ami = "14"
#BBFILE_PRIORITY_meta-ami = "6"

LAYERVERSION_meta-ami = "1"
LAYERSERIES_COMPAT_meta-ami = "honister kirkstone"

IMAGE_INSTALL:append = " pef-alert-manager mail-alert-manager adcapp pwmtachtool intel-ipmi-oem webui-vue virtual-media host-interface crashdump-trigger intelacd"

IMAGE_FEATURES += "obmc-user-mgmt-ldap \
                  "

IMAGE_INSTALL:remove += "crashdump"
