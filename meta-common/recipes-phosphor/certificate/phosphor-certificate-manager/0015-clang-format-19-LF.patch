From 1c82136dbd31b550950b1db237f506d1cefdaae4 Mon Sep 17 00:00:00 2001
From: Minal Upadhye <minalsunilu@ami.com>
Date: Thu, 26 Feb 2026 16:45:12 +0530
Subject: [PATCH] clang-format 19 LF

Signed-off-by: Minal Upadhye <minalsunilu@ami.com>
---
 certificate.cpp   |  35 ++--
 certs_manager.cpp | 407 ++++++++++++++++++++++++----------------------
 certs_manager.hpp |  58 +++----
 x509_utils.cpp    | 121 ++++++++------
 4 files changed, 329 insertions(+), 292 deletions(-)

diff --git a/certificate.cpp b/certificate.cpp
index 6b97470..6ff7115 100644
--- a/certificate.cpp
+++ b/certificate.cpp
@@ -7,6 +7,7 @@
 
 #include <openssl/asn1.h>
 #include <openssl/bio.h>
+#include <openssl/bn.h>
 #include <openssl/buffer.h>
 #include <openssl/err.h>
 #include <openssl/evp.h>
@@ -14,6 +15,7 @@
 #include <openssl/objects.h>
 #include <openssl/opensslv.h>
 #include <openssl/pem.h>
+#include <openssl/x509.h>
 #include <openssl/x509v3.h>
 
 #include <phosphor-logging/elog-errors.hpp>
@@ -32,9 +34,6 @@
 #include <map>
 #include <utility>
 #include <vector>
-#include <openssl/x509.h>
-#include <openssl/bn.h>
-
 
 namespace phosphor::certs
 {
@@ -46,13 +45,14 @@ using ::phosphor::logging::elog;
 using InvalidCertificateError =
     ::sdbusplus::xyz::openbmc_project::Certs::Error::InvalidCertificate;
 using ::phosphor::logging::xyz::openbmc_project::Certs::InvalidCertificate;
-using ::sdbusplus::xyz::openbmc_project::Common::Error::InternalFailure;
+using ::sdbusplus::xyz::openbmc_project::Certs::Error::
+    PrivateKeyCertificateFileNotMatch;
 using ::sdbusplus::xyz::openbmc_project::Certs::Error::PrivateKeyNotFound;
-using ::sdbusplus::xyz::openbmc_project::Certs::Error::PrivateKeyCertificateFileNotMatch;
-using PrivateKeyNotFoundReason = ::phosphor::logging::xyz::openbmc_project::Certs::PrivateKeyNotFound::REASON;
-using PrivateKeyCertificateFileNotMatchReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      PrivateKeyCertificateFileNotMatch::REASON;
-
+using ::sdbusplus::xyz::openbmc_project::Common::Error::InternalFailure;
+using PrivateKeyNotFoundReason = ::phosphor::logging::xyz::openbmc_project::
+    Certs::PrivateKeyNotFound::REASON;
+using PrivateKeyCertificateFileNotMatchReason = ::phosphor::logging::xyz::
+    openbmc_project::Certs::PrivateKeyCertificateFileNotMatch::REASON;
 
 // RAII support for openSSL functions.
 using BIOMemPtr = std::unique_ptr<BIO, decltype(&::BIO_free)>;
@@ -235,8 +235,9 @@ Certificate::Certificate(sdbusplus::bus_t& bus, const std::string& objPath,
     auto installHelper = [this](const auto& filePath) {
         if (!compareKeys(filePath))
         {
-            elog<PrivateKeyCertificateFileNotMatch>(PrivateKeyCertificateFileNotMatchReason(
-                "Private key does not match the Certificate"));
+            elog<PrivateKeyCertificateFileNotMatch>(
+                PrivateKeyCertificateFileNotMatchReason(
+                    "Private key does not match the Certificate"));
         };
     };
     typeFuncMap[CertificateType::server] = installHelper;
@@ -583,7 +584,8 @@ void Certificate::populateProperties(X509& cert)
     validNotBefore((days * dayToSeconds) + secs);
 
     EVP_PKEY* pkey = X509_get_pubkey(&cert);
-    uint16_t keySize = EVP_PKEY_bits(pkey);  // returns key size in bits, e.g., 2048
+    uint16_t keySize =
+        EVP_PKEY_bits(pkey); // returns key size in bits, e.g., 2048
     EVP_PKEY_free(pkey);
     publicKey(keySize);
 
@@ -596,14 +598,15 @@ void Certificate::populateProperties(X509& cert)
     OPENSSL_free(hex);
 
     int sig_nid = X509_get_signature_nid(&cert);
-    const char* sigAlgo = OBJ_nid2ln(sig_nid);  // long name (e.g., "sha256WithRSAEncryption")
+    const char* sigAlgo =
+        OBJ_nid2ln(sig_nid); // long name (e.g., "sha256WithRSAEncryption")
     std::string signatureAlgo(sigAlgo);
     signatureAlgorithm(signatureAlgo);
 
-    long version = X509_get_version(&cert);  // 0 = v1, 1 = v2, 2 = v3
-    std::string versionStr = std::to_string(version + 1);  // Convert to human-readable v1/v2/v3
+    long version = X509_get_version(&cert); // 0 = v1, 1 = v2, 2 = v3
+    std::string versionStr =
+        std::to_string(version + 1); // Convert to human-readable v1/v2/v3
     certificateVersion(versionStr);
-
 }
 
 void Certificate::checkAndAppendPrivateKey(const std::string& filePath)
diff --git a/certs_manager.cpp b/certs_manager.cpp
index 4c994c7..b5b3e05 100644
--- a/certs_manager.cpp
+++ b/certs_manager.cpp
@@ -5,6 +5,7 @@
 #include "x509_utils.hpp"
 
 #include <openssl/asn1.h>
+#include <openssl/bio.h>
 #include <openssl/bn.h>
 #include <openssl/ec.h>
 #include <openssl/err.h>
@@ -14,6 +15,7 @@
 #include <openssl/opensslv.h>
 #include <openssl/pem.h>
 #include <openssl/rsa.h>
+#include <openssl/x509.h>
 #include <unistd.h>
 
 #include <phosphor-logging/elog-errors.hpp>
@@ -27,10 +29,6 @@
 #include <xyz/openbmc_project/Certs/error.hpp>
 #include <xyz/openbmc_project/Common/error.hpp>
 
-#include <openssl/bio.h>
-#include <openssl/err.h>
-#include <openssl/x509.h>
-
 #include <algorithm>
 #include <array>
 #include <cerrno>
@@ -50,16 +48,16 @@ namespace
 namespace fs = std::filesystem;
 using namespace phosphor::logging;
 
+using ::sdbusplus::xyz::openbmc_project::Certs::Error::CertificateExists;
 using ::sdbusplus::xyz::openbmc_project::Certs::Error::InvalidCertificate;
 using ::sdbusplus::xyz::openbmc_project::Common::Error::InternalFailure;
 using ::sdbusplus::xyz::openbmc_project::Common::Error::NotAllowed;
-using ::sdbusplus::xyz::openbmc_project::Certs::Error::CertificateExists;
 using NotAllowedReason =
     ::phosphor::logging::xyz::openbmc_project::Common::NotAllowed::REASON;
 using InvalidCertificateReason = ::phosphor::logging::xyz::openbmc_project::
     Certs::InvalidCertificate::REASON;
-using CertificateExistsReason = ::phosphor::logging::xyz::openbmc_project::
-    Certs::CertificateExists::REASON;
+using CertificateExistsReason =
+    ::phosphor::logging::xyz::openbmc_project::Certs::CertificateExists::REASON;
 using ::sdbusplus::xyz::openbmc_project::Common::Error::InvalidArgument;
 using Argument =
     ::phosphor::logging::xyz::openbmc_project::Common::InvalidArgument;
@@ -82,7 +80,7 @@ constexpr std::string_view endCertificate = "-----END CERTIFICATE-----";
 using BIO_ptr = std::unique_ptr<BIO, decltype(&BIO_free)>;
 using X509_ptr = std::unique_ptr<X509, decltype(&X509_free)>;
 using ASN1_TIME_ptr = std::unique_ptr<ASN1_TIME, decltype(&ASN1_STRING_free)>;
-  
+
 /**
  * @brief Splits the given authorities list file and returns an array of
  * individual PEM encoded x509 certificate.
@@ -263,7 +261,8 @@ std::string Manager::install(const std::string filePath)
 {
     if (certType != CertificateType::authority && !installedCerts.empty())
     {
-        elog<CertificateExists>(CertificateExistsReason("Certificate already exist"));
+        elog<CertificateExists>(
+            CertificateExistsReason("Certificate already exist"));
     }
     else if (certType == CertificateType::authority &&
              installedCerts.size() >= maxNumAuthorityCertificates)
@@ -289,7 +288,8 @@ std::string Manager::install(const std::string filePath)
     }
     else
     {
-        elog<CertificateExists>(CertificateExistsReason("Certificate already exist"));
+        elog<CertificateExists>(
+            CertificateExistsReason("Certificate already exist"));
     }
 
     return certObjectPath;
@@ -469,8 +469,8 @@ void Manager::deleteCertificate(const Certificate* const certificate)
     const std::vector<std::unique_ptr<Certificate>>::iterator& certIt =
         std::find_if(installedCerts.begin(), installedCerts.end(),
                      [certificate](const std::unique_ptr<Certificate>& cert) {
-        return (cert.get() == certificate);
-    });
+                         return (cert.get() == certificate);
+                     });
 
     if (certType == CertificateType::server)
     {
@@ -523,7 +523,8 @@ void Manager::replaceCertificate(Certificate* const certificate,
             elog<NotAllowed>(NotAllowedReason("Certificate already exist"));
         }
     }
-    else if (contypestr == "xyz.openbmc_project.Certs.Certificate.Type.PEMchain")
+    else if (contypestr ==
+             "xyz.openbmc_project.Certs.Certificate.Type.PEMchain")
     {
         fs::path chainFile = fs::path(certInstallPath).parent_path();
         chainFile = chainFile / defaultHttpsChainListFileName;
@@ -712,10 +713,10 @@ void Manager::generateCSRHelper(
     lg2::info("Given Key pair algorithm, KEYPAIRALGORITHM:{KEYPAIRALGORITHM}",
               "KEYPAIRALGORITHM", keyPairAlgorithm);
 
-    if(isRenew() == true)
-      {
-	pKey = getPrivateKey( certInstallPath);
-      }
+    if (isRenew() == true)
+    {
+        pKey = getPrivateKey(certInstallPath);
+    }
     // Used EC algorithm as default if user did not give algorithm type.
     if (keyPairAlgorithm == "RSA")
         pKey = generateRSAKeyPair(keyBitLength);
@@ -988,68 +989,73 @@ void Manager::writePrivateKey(const EVPPkeyPtr& pKey,
 /***************************************
  * reKey - Create new key for generateCSR
  ***************************************/
-std::string Manager::reKey( int64_t keyBitLength,
-			    std::string keyCurveId,
-			    std::string keyPairAlgorithm)
+std::string Manager::reKey(int64_t keyBitLength, std::string keyCurveId,
+                           std::string keyPairAlgorithm)
 {
-  std::string csr;
-
-  // check if key info
-  if(keyPairAlgorithm == "RSA")
-    {
-      if(keyBitLength == supportedKeyBitLength)
-	{
-	  csrInfo.keyBitLength = keyBitLength;
-	}
-      else
-	{
-	  log<level::ERR>("Given Key bit length for RSA is not supported",
-			  entry("KEYBITLENGTH=%PRIu64", keyBitLength));
-	  elog<InternalFailure>();
-	}
-      csrInfo.keyCurveId = defaultKeyCurveID;
-    }
-  else if ((keyPairAlgorithm == "EC") || keyPairAlgorithm.empty())
-    {
-      if((keyCurveId == SN_secp384r1) || (keyCurveId == SN_secp521r1) || (keyCurveId == SN_X9_62_prime256v1) || keyCurveId == defaultKeyCurveID)
-	{
-	  csrInfo.keyCurveId = keyCurveId;
-	}
-      else
-	{
-	  log<level::ERR>("Given Key curve ID for EC is not supported. Supporting "
-			  "secp384r1, secp521r1, prime256v1, and secp224r1 only");
-	  elog<InvalidArgument>(Argument::ARGUMENT_NAME("KEYCURVEID"),
-				Argument::ARGUMENT_VALUE(keyCurveId.c_str()));
-	}
-      csrInfo.keyBitLength = defaultKeyBitLength;
-    }
-  else
+    std::string csr;
+
+    // check if key info
+    if (keyPairAlgorithm == "RSA")
+    {
+        if (keyBitLength == supportedKeyBitLength)
+        {
+            csrInfo.keyBitLength = keyBitLength;
+        }
+        else
+        {
+            log<level::ERR>("Given Key bit length for RSA is not supported",
+                            entry("KEYBITLENGTH=%PRIu64", keyBitLength));
+            elog<InternalFailure>();
+        }
+        csrInfo.keyCurveId = defaultKeyCurveID;
+    }
+    else if ((keyPairAlgorithm == "EC") || keyPairAlgorithm.empty())
+    {
+        if ((keyCurveId == SN_secp384r1) || (keyCurveId == SN_secp521r1) ||
+            (keyCurveId == SN_X9_62_prime256v1) ||
+            keyCurveId == defaultKeyCurveID)
+        {
+            csrInfo.keyCurveId = keyCurveId;
+        }
+        else
+        {
+            log<level::ERR>(
+                "Given Key curve ID for EC is not supported. Supporting "
+                "secp384r1, secp521r1, prime256v1, and secp224r1 only");
+            elog<InvalidArgument>(Argument::ARGUMENT_NAME("KEYCURVEID"),
+                                  Argument::ARGUMENT_VALUE(keyCurveId.c_str()));
+        }
+        csrInfo.keyBitLength = defaultKeyBitLength;
+    }
+    else
     {
         log<level::ERR>("Given Key pair algorithm is not supported. Supporting "
                         "RSA and EC only");
-        elog<InvalidArgument>(Argument::ARGUMENT_NAME("KEYPAIRALGORITHM"),
-			      Argument::ARGUMENT_VALUE(keyPairAlgorithm.c_str()));
-    }
-  
-  //load cert
-  X509_ptr cert(loadCert(certInstallPath));
-  //get the subject info from the current certificate
-  std::string subj = X509_NAME_oneline(X509_get_subject_name(cert.get()), NULL, 0);
-
-  //parse the subject
-  parseSSLCert(subj);
-
-  csrInfo.keyPairAlgorithm = keyPairAlgorithm;
-  
-  isRenew(false);
-  csr = generateCSR(csrInfo.altName, csrInfo.challengePassword,
-		    csrInfo.city, csrInfo.commonName, csrInfo.contactPerson,
-		    csrInfo.country, csrInfo.email, csrInfo.givenName,
-		    csrInfo.initial, csrInfo.keyBitLength, csrInfo.keyCurveId, csrInfo.keyPairAlgorithm,
-		    csrInfo.keyUsage,csrInfo.organization, csrInfo.organizationalUnit,
-		    csrInfo.state, csrInfo.surname, csrInfo.unstructuredName);
-  return csr;
+        elog<InvalidArgument>(
+            Argument::ARGUMENT_NAME("KEYPAIRALGORITHM"),
+            Argument::ARGUMENT_VALUE(keyPairAlgorithm.c_str()));
+    }
+
+    // load cert
+    X509_ptr cert(loadCert(certInstallPath));
+    // get the subject info from the current certificate
+    std::string subj =
+        X509_NAME_oneline(X509_get_subject_name(cert.get()), NULL, 0);
+
+    // parse the subject
+    parseSSLCert(subj);
+
+    csrInfo.keyPairAlgorithm = keyPairAlgorithm;
+
+    isRenew(false);
+    csr = generateCSR(
+        csrInfo.altName, csrInfo.challengePassword, csrInfo.city,
+        csrInfo.commonName, csrInfo.contactPerson, csrInfo.country,
+        csrInfo.email, csrInfo.givenName, csrInfo.initial, csrInfo.keyBitLength,
+        csrInfo.keyCurveId, csrInfo.keyPairAlgorithm, csrInfo.keyUsage,
+        csrInfo.organization, csrInfo.organizationalUnit, csrInfo.state,
+        csrInfo.surname, csrInfo.unstructuredName);
+    return csr;
 }
 
 /***************************************
@@ -1059,14 +1065,15 @@ std::string Manager::reNew()
 {
     std::string csr;
 
-    //Get certificate from file
+    // Get certificate from file
     BIO_ptr input(BIO_new(BIO_s_file()), BIO_free);
     if (BIO_read_filename(input.get(), certInstallPath.c_str()) <= 0)
     {
-      return std::string();
+        return std::string();
     }
     // Create an openssl certificate from the BIO
-    X509_ptr cert(PEM_read_bio_X509_AUX(input.get(), NULL, NULL, NULL), X509_free);
+    X509_ptr cert(PEM_read_bio_X509_AUX(input.get(), NULL, NULL, NULL),
+                  X509_free);
 
     // Create a BIO to hold info from the cert
     BIO_ptr output_bio(BIO_new(BIO_s_mem()), BIO_free);
@@ -1079,56 +1086,58 @@ std::string Manager::reNew()
     // Read one smaller than the buffer to make sure we end up with a null
     // terminator no matter what
     BIO_read(output_bio.get(), buffer, max_len - 1);
-    
+
     // Put the contents of the BIO into a C++ string
     std::string cert_details = std::string(buffer);
     BIO_reset(output_bio.get());
 
-    //get the subject
-    std::string subj = X509_NAME_oneline(X509_get_subject_name(cert.get()), NULL, 0);
-    //get issuer
-    //auto iss  = X509_NAME_oneline(X509_get_issuer_name(cert.get()), NULL, 0);
-    
+    // get the subject
+    std::string subj =
+        X509_NAME_oneline(X509_get_subject_name(cert.get()), NULL, 0);
+    // get issuer
+    // auto iss  = X509_NAME_oneline(X509_get_issuer_name(cert.get()), NULL, 0);
+
     parseSSLCert(subj);
 
-    //get the key info
-    if(cert_details.find("rsaEncryption") != std::string::npos)
-      {
-	csrInfo.keyPairAlgorithm = "RSA";
-	csrInfo.keyBitLength = defaultKeyBitLength; //only have one choice
-      }
-    else if(cert_details.find("id-ecPublicKey") != std::string::npos)
-      {
-	csrInfo.keyPairAlgorithm = "EC";
-	if(cert_details.find(SN_secp384r1))
-	  {
-	    csrInfo.keyCurveId = SN_secp384r1;
-	  }
-	else if(cert_details.find(SN_secp521r1))
-	  {
-	    csrInfo.keyCurveId = SN_secp521r1;
-	  }
-	else if(cert_details.find(SN_X9_62_prime256v1))
-	  {
-	    csrInfo.keyCurveId = SN_X9_62_prime256v1;
-	  }
-	else
-	  {
-	    return std::string();
-	  }
-      }
+    // get the key info
+    if (cert_details.find("rsaEncryption") != std::string::npos)
+    {
+        csrInfo.keyPairAlgorithm = "RSA";
+        csrInfo.keyBitLength = defaultKeyBitLength; // only have one choice
+    }
+    else if (cert_details.find("id-ecPublicKey") != std::string::npos)
+    {
+        csrInfo.keyPairAlgorithm = "EC";
+        if (cert_details.find(SN_secp384r1))
+        {
+            csrInfo.keyCurveId = SN_secp384r1;
+        }
+        else if (cert_details.find(SN_secp521r1))
+        {
+            csrInfo.keyCurveId = SN_secp521r1;
+        }
+        else if (cert_details.find(SN_X9_62_prime256v1))
+        {
+            csrInfo.keyCurveId = SN_X9_62_prime256v1;
+        }
+        else
+        {
+            return std::string();
+        }
+    }
     else
-      {
-	return std::string();
-      }
+    {
+        return std::string();
+    }
     isRenew(true);
-    csr = generateCSR(csrInfo.altName, csrInfo.challengePassword,
-		csrInfo.city, csrInfo.commonName, csrInfo.contactPerson,
-		csrInfo.country, csrInfo.email, csrInfo.givenName,
-		csrInfo.initial, csrInfo.keyBitLength, csrInfo.keyCurveId,
-		csrInfo.keyPairAlgorithm, csrInfo.keyUsage,csrInfo.organization, csrInfo.organizationalUnit, csrInfo.state,
-		csrInfo.surname, csrInfo.unstructuredName);
-    isRenew(false);    
+    csr = generateCSR(
+        csrInfo.altName, csrInfo.challengePassword, csrInfo.city,
+        csrInfo.commonName, csrInfo.contactPerson, csrInfo.country,
+        csrInfo.email, csrInfo.givenName, csrInfo.initial, csrInfo.keyBitLength,
+        csrInfo.keyCurveId, csrInfo.keyPairAlgorithm, csrInfo.keyUsage,
+        csrInfo.organization, csrInfo.organizationalUnit, csrInfo.state,
+        csrInfo.surname, csrInfo.unstructuredName);
+    isRenew(false);
     return csr;
 }
 
@@ -1137,81 +1146,85 @@ std::string Manager::reNew()
  ***************************************/
 int Manager::parseSSLCert(std::string subj)
 {
-  char target=0x2f; // char '/'
-  unsigned int i,m=1,n=0;
-
-  // adding a '/' to the end so parsing will find the last element
-  subj.push_back('/');
-  // starting at 1 because 0 is already '/'
-  for(i=1;i<subj.size();++i)
-    {
-      if(subj[i] == target)
-	{
-	  n = i;
-	  if(subj.substr(m,n-m).find("L=") != std::string::npos)
-	    {
-	      csrInfo.city = subj.substr(m+2,n-m-2);
-	    }
-	  else if(subj.substr(m,n-m).find("CN=") != std::string::npos)
-	    {
-	      csrInfo.commonName = subj.substr(m+3,n-m-3);
-	    }
-	  else if(subj.substr(m,n-m).find("name=") != std::string::npos)
-	    {
-	      csrInfo.contactPerson = subj.substr(m+5,n-m-5);
-	    }
-	  else if(subj.substr(m,n-m).find("C=") != std::string::npos)
-	    {
-	      csrInfo.country = subj.substr(m+2,n-m-2);
-	    }
-	  else if(subj.substr(m,n-m).find("emailAddress=") != std::string::npos)
-	    {
-	      csrInfo.email = subj.substr(m+13,n-m-13);
-	    }
-	  else if(subj.substr(m,n-m).find("GN=") != std::string::npos)
-	    {
-	      csrInfo.givenName = subj.substr(m+3,n-m-3);
-	    }
-	  else if(subj.substr(m,n-m).find("initial=") != std::string::npos)
-	    {
-	      csrInfo.initial = subj.substr(m+8,n-m-8);
-	    }
-	  else if(subj.substr(m,n-m).find("extendedKeyUsage=") != std::string::npos)
-	    {
-	      csrInfo.keyUsage.push_back(subj.substr(m+17,n-m-17));
-	    }
-	  else if(subj.substr(m,n-m).find("keyUsage=") != std::string::npos)
-	    {
-	      csrInfo.keyUsage.push_back(subj.substr(m+9,n-m-9));
-	    }
-	  else if(subj.substr(m,n-m).find("O=") != std::string::npos)
-	    {
-	      csrInfo.organization = subj.substr(m+2,n-m-2);
-	    }
-	  else if(subj.substr(m,n-m).find("OU=") != std::string::npos)
-	    {
-	      csrInfo.organizationalUnit = subj.substr(m+3,n-m-3);
-	    }
-	  else if(subj.substr(m,n-m).find("ST=") != std::string::npos)
-	    {
-	      csrInfo.state = subj.substr(m+3,n-m-3);
-	    }
-	  else if(subj.substr(m,n-m).find("SN=") != std::string::npos)
-	    {
-	      csrInfo.surname = subj.substr(m+3,n-m-3);
-	    }
-	  else if(subj.substr(m,n-m).find("unstructuredName=") != std::string::npos)
-	    {
-	      csrInfo.unstructuredName = subj.substr(m+3,n-m-3);
-	    }
-	  //reset for next string
-	  m=n+1;
-	  n=0;
-	}
-    }
-  
-  return 0;
+    char target = 0x2f; // char '/'
+    unsigned int i, m = 1, n = 0;
+
+    // adding a '/' to the end so parsing will find the last element
+    subj.push_back('/');
+    // starting at 1 because 0 is already '/'
+    for (i = 1; i < subj.size(); ++i)
+    {
+        if (subj[i] == target)
+        {
+            n = i;
+            if (subj.substr(m, n - m).find("L=") != std::string::npos)
+            {
+                csrInfo.city = subj.substr(m + 2, n - m - 2);
+            }
+            else if (subj.substr(m, n - m).find("CN=") != std::string::npos)
+            {
+                csrInfo.commonName = subj.substr(m + 3, n - m - 3);
+            }
+            else if (subj.substr(m, n - m).find("name=") != std::string::npos)
+            {
+                csrInfo.contactPerson = subj.substr(m + 5, n - m - 5);
+            }
+            else if (subj.substr(m, n - m).find("C=") != std::string::npos)
+            {
+                csrInfo.country = subj.substr(m + 2, n - m - 2);
+            }
+            else if (subj.substr(m, n - m).find("emailAddress=") !=
+                     std::string::npos)
+            {
+                csrInfo.email = subj.substr(m + 13, n - m - 13);
+            }
+            else if (subj.substr(m, n - m).find("GN=") != std::string::npos)
+            {
+                csrInfo.givenName = subj.substr(m + 3, n - m - 3);
+            }
+            else if (subj.substr(m, n - m).find("initial=") !=
+                     std::string::npos)
+            {
+                csrInfo.initial = subj.substr(m + 8, n - m - 8);
+            }
+            else if (subj.substr(m, n - m).find("extendedKeyUsage=") !=
+                     std::string::npos)
+            {
+                csrInfo.keyUsage.push_back(subj.substr(m + 17, n - m - 17));
+            }
+            else if (subj.substr(m, n - m).find("keyUsage=") !=
+                     std::string::npos)
+            {
+                csrInfo.keyUsage.push_back(subj.substr(m + 9, n - m - 9));
+            }
+            else if (subj.substr(m, n - m).find("O=") != std::string::npos)
+            {
+                csrInfo.organization = subj.substr(m + 2, n - m - 2);
+            }
+            else if (subj.substr(m, n - m).find("OU=") != std::string::npos)
+            {
+                csrInfo.organizationalUnit = subj.substr(m + 3, n - m - 3);
+            }
+            else if (subj.substr(m, n - m).find("ST=") != std::string::npos)
+            {
+                csrInfo.state = subj.substr(m + 3, n - m - 3);
+            }
+            else if (subj.substr(m, n - m).find("SN=") != std::string::npos)
+            {
+                csrInfo.surname = subj.substr(m + 3, n - m - 3);
+            }
+            else if (subj.substr(m, n - m).find("unstructuredName=") !=
+                     std::string::npos)
+            {
+                csrInfo.unstructuredName = subj.substr(m + 3, n - m - 3);
+            }
+            // reset for next string
+            m = n + 1;
+            n = 0;
+        }
+    }
 
+    return 0;
 }
 
 /***************************************
@@ -1230,18 +1243,18 @@ EVPPkeyPtr Manager::getPrivateKey(const std::string& privKeyFileName)
         log<level::ERR>("Error occurred getting private key file");
         elog<InternalFailure>();
     }
-    
+
     // read private key
     EVP_PKEY* pKey = nullptr;
 
-    if(! (pKey = PEM_read_PrivateKey(fp, nullptr, nullptr, nullptr)))
+    if (!(pKey = PEM_read_PrivateKey(fp, nullptr, nullptr, nullptr)))
     {
         std::fclose(fp);
         log<level::ERR>("Error occurred while reading private key");
         elog<InternalFailure>();
     }
     std::fclose(fp);
-    
+
     return {pKey, &::EVP_PKEY_free};
 }
 
diff --git a/certs_manager.hpp b/certs_manager.hpp
index 0146c2c..7f7db6d 100644
--- a/certs_manager.hpp
+++ b/certs_manager.hpp
@@ -202,7 +202,7 @@ class Manager : public internal::ManagerInterface
         std::string unstructuredName) override;
 
     std::string reNew() override;
-     /** @brief Generate CSR with new key using current Certificate credentials
+    /** @brief Generate CSR with new key using current Certificate credentials
      *
      *  @param[in] keyBitLength - The length of the key in bits, if needed based
      *      on the value of the KeyPairAlgorithm parameter.
@@ -213,15 +213,14 @@ class Manager : public internal::ManagerInterface
      *      generation are: RSA, DSA, DH and EC.
      *  @return Reference to certificates' collection
      */
-    std::string reKey(int64_t keyBitLength,
-                     std::string keyCurveId,
-                     std::string keyPairAlgorithm) override;
+    std::string reKey(int64_t keyBitLength, std::string keyCurveId,
+                      std::string keyPairAlgorithm) override;
 
-     /** @brief Generate CSR with current key and current Certificate credentials
+    /** @brief Generate CSR with current key and current Certificate credentials
      *
      *  @return Reference to certificates' collection
      */
-     //std::string isRenew();
+    // std::string isRenew();
 
     /** @brief Get reference to certificates' collection
      *
@@ -274,8 +273,8 @@ class Manager : public internal::ManagerInterface
      *  @param[in] privKeyFileName - private key filename
      *  @return pKey - pointer to private key
      */
-    std::unique_ptr<EVP_PKEY, decltype(&::EVP_PKEY_free)>
-    getPrivateKey(const std::string& privKeyFileName);
+    std::unique_ptr<EVP_PKEY, decltype(&::EVP_PKEY_free)> getPrivateKey(
+        const std::string& privKeyFileName);
 
     /** @brief Add the specified CSR field with the data
      *  @param[in] x509Name - Structure used in setting certificate properties
@@ -323,7 +322,7 @@ class Manager : public internal::ManagerInterface
     std::unique_ptr<EVP_PKEY, decltype(&::EVP_PKEY_free)> getRSAKeyPair(
         const int64_t keyBitLength);
 
-     /** @brief Parsing Certificate Subject
+    /** @brief Parsing Certificate Subject
      *  Parsing Certificate Subject
      *  @param[in]  subj - subject of certficate
      *  @return     success/failure
@@ -382,25 +381,26 @@ class Manager : public internal::ManagerInterface
 
     /** @brief Certificate ID pool */
     uint64_t certIdCounter = 1;
-  struct {
-    std::vector<std::string> altName; //subjectAltName
-    std::string challengePassword;  //challengePassword
-    std::string city;               //L
-    std::string commonName;         //CN
-    std::string contactPerson;      //name
-    std::string country;            //C
-    std::string email;              //emailAddress
-    std::string givenName;          //GN
-    std::string initial;            //initial
-    uint64_t keyBitLength;          //
-    std::string keyCurveId;         //
-    std::string keyPairAlgorithm;   //algorithm
-    std::string organization;       //O
-    std::string organizationalUnit; //OU
-    std::string state;              //ST
-    std::string surname;            //SN
-    std::string unstructuredName;   //unstructuredName
-    std::vector<std::string> keyUsage; //keyUsage + extendedKeyUsage
-  } csrInfo;
+    struct
+    {
+        std::vector<std::string> altName;  // subjectAltName
+        std::string challengePassword;     // challengePassword
+        std::string city;                  // L
+        std::string commonName;            // CN
+        std::string contactPerson;         // name
+        std::string country;               // C
+        std::string email;                 // emailAddress
+        std::string givenName;             // GN
+        std::string initial;               // initial
+        uint64_t keyBitLength;             //
+        std::string keyCurveId;            //
+        std::string keyPairAlgorithm;      // algorithm
+        std::string organization;          // O
+        std::string organizationalUnit;    // OU
+        std::string state;                 // ST
+        std::string surname;               // SN
+        std::string unstructuredName;      // unstructuredName
+        std::vector<std::string> keyUsage; // keyUsage + extendedKeyUsage
+    } csrInfo;
 };
 } // namespace phosphor::certs
diff --git a/x509_utils.cpp b/x509_utils.cpp
index 62a4f87..659b023 100644
--- a/x509_utils.cpp
+++ b/x509_utils.cpp
@@ -28,34 +28,38 @@ namespace
 {
 
 using ::phosphor::logging::elog;
+using ::sdbusplus::xyz::openbmc_project::Certs::Error::CertificateFileExpired;
+using ::sdbusplus::xyz::openbmc_project::Certs::Error::
+    CertificateFileSizeExceeded;
+using ::sdbusplus::xyz::openbmc_project::Certs::Error::CertificateFileUntrusted;
+using ::sdbusplus::xyz::openbmc_project::Certs::Error::
+    CertificateKeyLengthTooSmall;
 using ::sdbusplus::xyz::openbmc_project::Certs::Error::InvalidCertificate;
-using ::sdbusplus::xyz::openbmc_project::Common::Error::InternalFailure;
-using ::sdbusplus::xyz::openbmc_project::Certs::Error::CertificateKeyLengthTooSmall;
-using ::sdbusplus::xyz::openbmc_project::Certs::Error::CertificateFileSizeExceeded;
 using ::sdbusplus::xyz::openbmc_project::Certs::Error::PrivateFileSizeExceeded;
-using ::sdbusplus::xyz::openbmc_project::Certs::Error::CertificateFileUntrusted;
-using ::sdbusplus::xyz::openbmc_project::Certs::Error::VerifyCertificateFileFailed;
-using ::sdbusplus::xyz::openbmc_project::Certs::Error::PrivateKeyCertificateFileNotMatch;
-using ::sdbusplus::xyz::openbmc_project::Certs::Error::CertificateFileExpired;
+using ::sdbusplus::xyz::openbmc_project::Certs::Error::
+    PrivateKeyCertificateFileNotMatch;
 using ::sdbusplus::xyz::openbmc_project::Certs::Error::PrivateKeyFileEncrypted;
+using ::sdbusplus::xyz::openbmc_project::Certs::Error::
+    VerifyCertificateFileFailed;
+using ::sdbusplus::xyz::openbmc_project::Common::Error::InternalFailure;
 using Reason = ::phosphor::logging::xyz::openbmc_project::Certs::
     InvalidCertificate::REASON;
-using CertificateKeyLengthTooSmallReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      CertificateKeyLengthTooSmall::REASON;
-using CertificateFileSizeExceededReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      CertificateFileSizeExceeded::REASON;
-using PrivateFileSizeExceededReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      PrivateFileSizeExceeded::REASON;
-using CertificateFileUntrustedReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      CertificateFileUntrusted::REASON;
-using VerifyCertificateFileFailedReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      VerifyCertificateFileFailed::REASON;
-using PrivateKeyCertificateFileNotMatchReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      PrivateKeyCertificateFileNotMatch::REASON;
-using CertificateFileExpiredReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      CertificateFileExpired::REASON;
-using PrivateKeyFileEncryptedReason = ::phosphor::logging::xyz::openbmc_project::Certs::
-      PrivateKeyFileEncrypted::REASON;
+using CertificateKeyLengthTooSmallReason = ::phosphor::logging::xyz::
+    openbmc_project::Certs::CertificateKeyLengthTooSmall::REASON;
+using CertificateFileSizeExceededReason = ::phosphor::logging::xyz::
+    openbmc_project::Certs::CertificateFileSizeExceeded::REASON;
+using PrivateFileSizeExceededReason = ::phosphor::logging::xyz::
+    openbmc_project::Certs::PrivateFileSizeExceeded::REASON;
+using CertificateFileUntrustedReason = ::phosphor::logging::xyz::
+    openbmc_project::Certs::CertificateFileUntrusted::REASON;
+using VerifyCertificateFileFailedReason = ::phosphor::logging::xyz::
+    openbmc_project::Certs::VerifyCertificateFileFailed::REASON;
+using PrivateKeyCertificateFileNotMatchReason = ::phosphor::logging::xyz::
+    openbmc_project::Certs::PrivateKeyCertificateFileNotMatch::REASON;
+using CertificateFileExpiredReason = ::phosphor::logging::xyz::openbmc_project::
+    Certs::CertificateFileExpired::REASON;
+using PrivateKeyFileEncryptedReason = ::phosphor::logging::xyz::
+    openbmc_project::Certs::PrivateKeyFileEncrypted::REASON;
 
 // RAII support for openSSL functions.
 using X509StorePtr = std::unique_ptr<X509_STORE, decltype(&::X509_STORE_free)>;
@@ -161,8 +165,8 @@ void validateCertificateStartDate(X509& cert)
     if (days < 0 || secs < 0)
     {
         lg2::error("Certificate valid date starts before the Unix Epoch");
-        elog<VerifyCertificateFileFailed>(
-            VerifyCertificateFileFailedReason("NotBefore should after 19700101000000Z"));
+        elog<VerifyCertificateFileFailed>(VerifyCertificateFileFailedReason(
+            "NotBefore should after 19700101000000Z"));
     }
 }
 
@@ -171,12 +175,14 @@ int validateCertificateKeyType(X509& cert)
     EVPPkeyTypePtr pubKey(X509_get_pubkey(&cert), ::EVP_PKEY_free);
     if (!pubKey)
     {
-        lg2::error("X509_get_pubkey() failed, ERRCODE:{ERRCODE}", "ERRCODE",  ERR_get_error());
+        lg2::error("X509_get_pubkey() failed, ERRCODE:{ERRCODE}", "ERRCODE",
+                   ERR_get_error());
         elog<InvalidCertificate>(Reason("Failed to get public key info"));
     }
     int keyType = EVP_PKEY_id(pubKey.get());
     int pkeyType = EVP_PKEY_type(keyType);
-    lg2::info("Certificate cryptographic keyType, KEYTYPE:{KEYTYPE}", "KEYTYPE",  pkeyType);
+    lg2::info("Certificate cryptographic keyType, KEYTYPE:{KEYTYPE}", "KEYTYPE",
+              pkeyType);
     return pkeyType;
 }
 void validateCertificateKeyLength(X509& cert)
@@ -184,7 +190,8 @@ void validateCertificateKeyLength(X509& cert)
     EVPPkeyPtr pubKey(X509_get_pubkey(&cert), ::EVP_PKEY_free);
     if (!pubKey)
     {
-        lg2::error("X509_get_pubkey() failed, ERRCODE:{ERRCODE}", "ERRCODE",  ERR_get_error());
+        lg2::error("X509_get_pubkey() failed, ERRCODE:{ERRCODE}", "ERRCODE",
+                   ERR_get_error());
         elog<InvalidCertificate>(Reason("Failed to get public key info"));
     }
     int minKeyBitLength = 0;
@@ -203,20 +210,29 @@ void validateCertificateKeyLength(X509& cert)
     }
     else
     {
-        lg2::error("Invalid cryptographic KeyType certificate uploaded, KEYTYPE:{KEYTYPE}", "KEYTYPE",  pkeyType);
+        lg2::error(
+            "Invalid cryptographic KeyType certificate uploaded, KEYTYPE:{KEYTYPE}",
+            "KEYTYPE", pkeyType);
         elog<InvalidCertificate>(Reason("Invalid key type certificate"));
     }
-    lg2::info("Certificate cryptographic length, KEYLENGTH:{KEYLENGTH}", "KEYLENGTH",  keyLen);
+    lg2::info("Certificate cryptographic length, KEYLENGTH:{KEYLENGTH}",
+              "KEYLENGTH", keyLen);
 
     if (keyLen < minKeyBitLength)
     {
-        lg2::error("Invalid cryptographic length certificate uploaded, KEYLENGTH:{KEYLENGTH}", "KEYLENGTH",  keyLen);
-        elog<CertificateKeyLengthTooSmall>(CertificateKeyLengthTooSmallReason("Invalid key length certificate"));
+        lg2::error(
+            "Invalid cryptographic length certificate uploaded, KEYLENGTH:{KEYLENGTH}",
+            "KEYLENGTH", keyLen);
+        elog<CertificateKeyLengthTooSmall>(CertificateKeyLengthTooSmallReason(
+            "Invalid key length certificate"));
     }
-    else if(keyLen > maxKeyBitLength)
+    else if (keyLen > maxKeyBitLength)
     {
-        lg2::error("Invalid cryptographic length certificate uploaded, KEYLENGTH:{KEYLENGTH}", "KEYLENGTH",  keyLen);
-        elog<CertificateFileSizeExceeded>(CertificateFileSizeExceededReason("Invalid key length certificate"));
+        lg2::error(
+            "Invalid cryptographic length certificate uploaded, KEYLENGTH:{KEYLENGTH}",
+            "KEYLENGTH", keyLen);
+        elog<CertificateFileSizeExceeded>(CertificateFileSizeExceededReason(
+            "Invalid key length certificate"));
     }
 }
 
@@ -277,7 +293,8 @@ void validateCertificateAgainstStore(X509_STORE& x509Store, X509& cert)
         if (errCode == X509_V_ERR_CERT_HAS_EXPIRED)
         {
             lg2::error("Expired certificate ");
-            elog<CertificateFileExpired>(CertificateFileExpiredReason("Expired Certificate"));
+            elog<CertificateFileExpired>(
+                CertificateFileExpiredReason("Expired Certificate"));
         }
         // Logging general error here.
         lg2::error(
@@ -340,31 +357,35 @@ std::unique_ptr<X509, decltype(&::X509_free)> parseCert(const std::string& pem)
     return cert;
 }
 
-void validatePrivateKeyEncryption(const std::string& pemFilePath){
-
-    FILE *pemFilePtr = fopen(pemFilePath.c_str(),"r");
-    if (!pemFilePtr) {
-
+void validatePrivateKeyEncryption(const std::string& pemFilePath)
+{
+    FILE* pemFilePtr = fopen(pemFilePath.c_str(), "r");
+    if (!pemFilePtr)
+    {
         lg2::error("error reading PEM file.");
         elog<InvalidCertificate>(Reason("Unable to open Pem file"));
     }
 
     EVP_PKEY* pkey = PEM_read_PrivateKey(pemFilePtr, nullptr, nullptr, nullptr);
 
-    if(pkey == nullptr)
+    if (pkey == nullptr)
     {
         lg2::error("Private key is encrypted/Failed to read the private key");
-        elog<PrivateKeyFileEncrypted>(PrivateKeyFileEncryptedReason("PrivateKey is encrypted"));
+        elog<PrivateKeyFileEncrypted>(
+            PrivateKeyFileEncryptedReason("PrivateKey is encrypted"));
     }
 
-   int maxKeyBitLength = 4096;
-   int keyLength = EVP_PKEY_bits(pkey);
+    int maxKeyBitLength = 4096;
+    int keyLength = EVP_PKEY_bits(pkey);
 
-   if(keyLength > maxKeyBitLength)
-   {
-        lg2::error("Invalid cryptographic length certificate uploaded, KEYLENGTH:{KEYLENGTH}", "KEYLENGTH",  keyLength);
-        elog<PrivateFileSizeExceeded>(PrivateFileSizeExceededReason("Invalid cryptographic length PrivateKey uploaded, KEYLENGTH:{KEYLENGTH} KEYLENGTH :"));
-   }
+    if (keyLength > maxKeyBitLength)
+    {
+        lg2::error(
+            "Invalid cryptographic length certificate uploaded, KEYLENGTH:{KEYLENGTH}",
+            "KEYLENGTH", keyLength);
+        elog<PrivateFileSizeExceeded>(PrivateFileSizeExceededReason(
+            "Invalid cryptographic length PrivateKey uploaded, KEYLENGTH:{KEYLENGTH} KEYLENGTH :"));
+    }
 
     EVP_PKEY_free(pkey);
 }
-- 
2.34.1

