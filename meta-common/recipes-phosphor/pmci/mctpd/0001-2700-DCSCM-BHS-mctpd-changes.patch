--- a/include/utils/utils.hpp	2025-04-07 06:25:52.447678344 +0000
+++ b/include/utils/utils.hpp	2025-04-07 06:45:54.185247449 +0000
@@ -204,8 +204,12 @@
 {
     if (fd)
     {
+#if defined (__aarch64__)
+        close(static_cast<int>(reinterpret_cast<long>(fd)));
+#else
         close(reinterpret_cast<long>(fd));
+#endif
     }
 }
 
-using FileHandle = std::unique_ptr<int, std::function<void(int*)>>;
\ No newline at end of file
+using FileHandle = std::unique_ptr<int, std::function<void(int*)>>;
--- a/src/hw/aspeed/I3CDriver.cpp	2025-04-07 06:26:12.186050697 +0000
+++ b/src/hw/aspeed/I3CDriver.cpp	2025-04-07 06:40:08.621353712 +0000
@@ -84,7 +84,11 @@
             phosphor::logging::log<phosphor::logging::level::INFO>(
                 "Running rescan");
             const char* writeData = "1";
+#if defined(__aarch64__)
+	    size_t status = write(fd, reinterpret_cast<const void*>(writeData), 1);
+#else
             int status = write(fd, reinterpret_cast<const void*>(writeData), 1);
+#endif
             if (status != 1)
             {
                 phosphor::logging::log<phosphor::logging::level::ERR>(
@@ -182,7 +186,11 @@
                     "Error registering MCTP o. I3C default client");
                 return;
             }
+#if defined(__aarch64__)
+	    streamMonitor.assign(static_cast<int>(reinterpret_cast<long>(handle.release())));
+#else
             streamMonitor.assign(reinterpret_cast<int>(handle.release()));
+#endif
             break;
         }
         else
@@ -299,4 +307,4 @@
     return getStatus(i3cDeviceFile, status);
 }
 } // namespace aspeed
-} // namespace hw
\ No newline at end of file
+} // namespace hw
--- a/CMakeLists.txt	2025-04-07 09:46:37.304680404 +0000
+++ b/CMakeLists.txt	2025-04-07 09:46:48.446680386 +0000
@@ -25,7 +25,6 @@
     -Woverloaded-virtual \
     -Wpedantic \
     -Wconversion \
-    -Wnull-dereference \
     -Wdouble-promotion \
     -Wformat=2 \
 ")
