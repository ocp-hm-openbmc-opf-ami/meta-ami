From 90e088f326c7095f86e75728103743c2b1c956b5 Mon Sep 17 00:00:00 2001
From: ramsankarr <ramsankarr@ami.com>
Date: Sun, 30 Mar 2025 02:49:26 +0530
Subject: [PATCH 69/69] MemoryLeakIssueAddressing

---
 snmp_client.cpp       |  2 +-
 snmp_notification.cpp | 20 +++++++++++---------
 snmp_util.cpp         |  4 ----
 3 files changed, 12 insertions(+), 14 deletions(-)

diff --git a/snmp_client.cpp b/snmp_client.cpp
index de8303e..13edf4d 100644
--- a/snmp_client.cpp
+++ b/snmp_client.cpp
@@ -118,7 +118,7 @@ std::string Client::user(std::string value)
     {
       while(gr != nullptr)
 	{
-	  if(strcmp(gr->gr_name,"snmp") == 0)
+	  if((strcmp(gr->gr_name,"snmp") == 0) && (gr->gr_mem != nullptr))
 	    {
 	      int i=0;
 	      while(gr->gr_mem[i] != nullptr)
diff --git a/snmp_notification.cpp b/snmp_notification.cpp
index 4837eb7..639bae4 100644
--- a/snmp_notification.cpp
+++ b/snmp_notification.cpp
@@ -14,9 +14,9 @@ namespace phosphor
     {
         namespace snmp
         {
-            std::string snmpConfFilePath = "/etc/snmp/snmp.conf";
-            std::string snmpTrapCommStrFilePath = "/etc/snmp/snmpTrapCommStr";
-            std::string snmpTrapStatusFilePath = "/etc/snmp/SnmpTrapStatus";
+            constexpr std::string_view snmpConfFilePath = "/etc/snmp/snmp.conf";
+            constexpr std::string_view snmpTrapCommStrFilePath = "/etc/snmp/snmpTrapCommStr";
+            constexpr std::string_view snmpTrapStatusFilePath = "/etc/snmp/SnmpTrapStatus";
             using namespace phosphor::logging;
             using namespace sdbusplus::xyz::openbmc_project::Common::Error;
 
@@ -57,6 +57,9 @@ namespace phosphor
                                     value.c_str(), value.length());
                         }
                         break;
+                    default:
+                        varList = nullptr;
+                        break;
                 }
                 return (varList == nullptr ? false : true);
             }
@@ -67,7 +70,7 @@ namespace phosphor
                 std::string line;
                 std::string key, value;
 
-                std::ifstream snmpConfFileStream(snmpConfFilePath);
+                std::ifstream snmpConfFileStream(snmpConfFilePath.data());
 
                 if (!snmpConfFileStream.is_open()) {
                     return disableStatus;
@@ -108,7 +111,7 @@ namespace phosphor
                 std::string commStr;
                 std::map<std::string, bool> snmpDisableStatus = getSNMPDisableStatus();
 
-                std::ifstream snmpTrapStatusFileStream(snmpTrapStatusFilePath, std::ios::in);
+                std::ifstream snmpTrapStatusFileStream(snmpTrapStatusFilePath.data(), std::ios::in);
                 if (!snmpTrapStatusFileStream.is_open()) {    
                     lg2::error("Failed to open /etc/snmp/SnmpTrapStatus file");
                     return;
@@ -121,7 +124,7 @@ namespace phosphor
                     return;
                 }
 
-                std::ifstream snmpTrapCommStrFileStream(snmpTrapCommStrFilePath);
+                std::ifstream snmpTrapCommStrFileStream(snmpTrapCommStrFilePath.data());
                 if (snmpTrapCommStrFileStream.is_open()) {
                     getline(snmpTrapCommStrFileStream, commStr);
                     snmpTrapCommStrFileStream.close();
@@ -139,7 +142,6 @@ namespace phosphor
                     }
                     if((mgr.version == "v1") || (mgr.version == "v2c"))
                     {
-                                    lg2::error("9626_1 {VERSION} ","VERSION",mgr.version);
                                 if(mgr.version == "v1")
                                 {
                                     session.version = SNMP_VERSION_1;
@@ -159,7 +161,6 @@ namespace phosphor
                     }
                     else if (mgr.version == "v3")
                     {
-                                    lg2::error("9626_3 {USER} ","USER",mgr.user);
                                 session.version = SNMP_VERSION_3;
                                 /* set the SNMPv3 user name and check if the user is available*/
                                 session.securityName = mgr.user.data();
@@ -169,7 +170,7 @@ namespace phosphor
 
                                 while(gr != nullptr)
                                 {
-                                    if(strcmp(gr->gr_name,"snmp") == 0)
+                                    if((strcmp(gr->gr_name,"snmp") == 0) && (gr->gr_mem != nullptr))
                                     {
                                         int i=0;
                                         while(gr->gr_mem[i] != nullptr)
@@ -261,6 +262,7 @@ namespace phosphor
                     if (!pdu)
                     {
                         lg2::error("Failed to create notification PDU");
+                        snmp_free_pdu(pdu);
                         continue;
                     }
 
diff --git a/snmp_util.cpp b/snmp_util.cpp
index 7b986aa..5015ea7 100644
--- a/snmp_util.cpp
+++ b/snmp_util.cpp
@@ -10,7 +10,6 @@
 #include <iostream>
 #include <fstream>
 #include <string>
-const std::string tempChassisFilepath = "/tmp/chassis.tmp";
 
 namespace phosphor
 {
@@ -24,9 +23,6 @@ ObjectValueTree getManagedObjects(sdbusplus::bus_t& bus,
 {
     std::ofstream fpchassis;
     ObjectValueTree interfaces;
- syslog(LOG_EMERG , "9626 service = %s", service.c_str());
- syslog(LOG_ALERT , "9626_object path = %s", objPath.c_str());
-
     auto method = bus.new_method_call(service.c_str(), objPath.c_str(),
                                       "org.freedesktop.DBus.ObjectManager",
                                       "GetManagedObjects");
-- 
2.25.1

