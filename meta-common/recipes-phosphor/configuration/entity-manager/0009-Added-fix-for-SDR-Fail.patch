From 687f5ce6eca9596a46a81058bc83ae29a2de587b Mon Sep 17 00:00:00 2001
From: nikhilad <nikhilad@ami.com>
Date: Thu, 30 Oct 2025 23:08:40 +0530
Subject: [PATCH] Added fix for SDR Fail.

Recreated the patch
Working as expected.

Signed-off-by: saravananm <saravananm@ami.com>

Signed-off-by: nikhilad <nikhilad@ami.com>
---
 src/fru_device/fru_device.cpp | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/src/fru_device/fru_device.cpp b/src/fru_device/fru_device.cpp
index c0df905..7aa9214 100644
--- a/src/fru_device/fru_device.cpp
+++ b/src/fru_device/fru_device.cpp
@@ -1382,7 +1382,7 @@ void rescanBusses(
     std::shared_ptr<sdbusplus::asio::connection>& systemBus)
 {
     static boost::asio::steady_timer timer(io);
-    timer.expires_after(std::chrono::seconds(1));
+    timer.expires_after(std::chrono::seconds(5));
 
     // setup an async wait in case we get flooded with requests
     timer.async_wait([&](const boost::system::error_code& ec) {
-- 
2.34.1

