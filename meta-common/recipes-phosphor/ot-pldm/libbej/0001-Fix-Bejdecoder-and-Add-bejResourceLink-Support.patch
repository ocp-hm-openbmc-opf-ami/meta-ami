From 0e9512d4c10dbbbf86a775d70b76b6c845bf2016 Mon Sep 17 00:00:00 2001
From: henryliu <henryliu@ami.com>
Date: Wed, 10 Sep 2025 17:38:05 +0800
Subject: [PATCH] Fix-Bejdecoder-and-Add-bejResourceLink-Support

---
 include/libbej/bej_decoder_core.h   |  7 +++
 include/libbej/bej_decoder_json.hpp |  8 ++-
 include/libbej/meson.build          |  3 +-
 src/bej_decoder_core.c              | 86 +++++++++++++++++++++++++++--
 src/bej_decoder_json.cpp            | 52 ++++++++++++++++-
 5 files changed, 145 insertions(+), 11 deletions(-)

diff --git a/include/libbej/bej_decoder_core.h b/include/libbej/bej_decoder_core.h
index 919c396..8426466 100644
--- a/include/libbej/bej_decoder_core.h
+++ b/include/libbej/bej_decoder_core.h
@@ -127,6 +127,12 @@ struct BejDecodedCallback
      */
     int (*callbackAnnotation)(const char* propertyName, void* dataPtr);
 
+    /**
+     * @brief Calls when a ResourceLink property is found.
+     */
+    int (*callbackResourceLink)(const char* propertyName, uint64_t resourceId,
+                                void* dataPtr);
+
     /**
      * @brief Calls when a read only property is found.
      */
@@ -182,6 +188,7 @@ struct BejHandleTypeFuncParam
     const struct BejStackCallback* stackCallback;
     void* callbacksDataPtr;
     void* stackDataPtr;
+    bool isCommaNeeded;
 };
 
 /**
diff --git a/include/libbej/bej_decoder_json.hpp b/include/libbej/bej_decoder_json.hpp
index 21c25d6..06f5587 100644
--- a/include/libbej/bej_decoder_json.hpp
+++ b/include/libbej/bej_decoder_json.hpp
@@ -5,6 +5,7 @@
 
 #include <span>
 #include <string>
+#include <unordered_map>
 #include <vector>
 
 namespace libbej
@@ -21,10 +22,13 @@ class BejDecoderJson
      *
      * @param[in] dictionaries - dictionaries needed for decoding.
      * @param[in] encodedPldmBlock - encoded PLDM block.
+     * @param[in] resourceIdToUriMap - mapping of resource IDs to URIs.
      * @return 0 if successful.
      */
-    int decode(const BejDictionaries& dictionaries,
-               const std::span<const uint8_t> encodedPldmBlock);
+    int decode(
+        const BejDictionaries& dictionaries,
+        const std::span<const uint8_t> encodedPldmBlock,
+        const std::unordered_map<uint32_t, std::string>& resourceIdToUriMap);
 
     /**
      * @brief Get the JSON output related to the latest call to decode.
diff --git a/include/libbej/meson.build b/include/libbej/meson.build
index 6866643..e8504e3 100644
--- a/include/libbej/meson.build
+++ b/include/libbej/meson.build
@@ -5,7 +5,8 @@ libbej_headers = files(
   'bej_common.h',
   'bej_encoder_core.h',
   'bej_encoder_metadata.h',
-  'bej_encoder_json.hpp'
+  'bej_encoder_json.hpp',
+  'bej_tree.h'
 )
 
 install_headers(
diff --git a/src/bej_decoder_core.c b/src/bej_decoder_core.c
index 09a9430..4043f5c 100644
--- a/src/bej_decoder_core.c
+++ b/src/bej_decoder_core.c
@@ -124,8 +124,8 @@ static void bejInitSFLVStruct(struct BejHandleTypeFuncParam* params)
  * @param[in] params - a valid BejHandleTypeFuncParam struct.
  * @return offset with respect to the start of the encoded stream.
  */
-static uint32_t
-    bejGetFirstTupleOffset(const struct BejHandleTypeFuncParam* params)
+static uint32_t bejGetFirstTupleOffset(
+    const struct BejHandleTypeFuncParam* params)
 {
     struct BejSFLVOffset localOffset;
     // Get the offset of the value with respect to the current encoded segment
@@ -176,6 +176,24 @@ static int bejGetDictionaryAndProperty(
 
     int ret =
         bejDictGetProperty(*dictionary, dictPropOffset, sequenceNumber, prop);
+    if (ret == bejErrorUnknownProperty || schemaType == bejAnnotation)
+    {
+        if (schemaType == bejAnnotation)
+        {
+            dictPropOffset = bejDictGetFirstAnnotatedPropertyOffset();
+        }
+        else
+        {
+            dictPropOffset = bejDictGetPropertyHeadOffset();
+        }
+        fprintf(
+            stderr,
+            "Unknown property with sequence number %u, retry from dict offset= %u\n",
+            sequenceNumber, dictPropOffset);
+        ret = bejDictGetProperty(*dictionary, dictPropOffset, sequenceNumber,
+                                 prop);
+    }
+
     if (ret != 0)
     {
         fprintf(stderr, "Failed to get dictionary property for offset: %u\n",
@@ -246,6 +264,7 @@ static int bejProcessEnding(struct BejHandleTypeFuncParam* params,
             params->state.mainDictPropOffset = ending->mainDictPropOffset;
             params->state.annoDictPropOffset = ending->annoDictPropOffset;
             params->state.addPropertyName = ending->addPropertyName;
+            params->isCommaNeeded = true;
 
             if (ending->sectionType == bejSectionSet)
             {
@@ -261,7 +280,7 @@ static int bejProcessEnding(struct BejHandleTypeFuncParam* params,
             }
             params->stackCallback->stackPop(params->stackDataPtr);
         }
-        else
+        else if (params->isCommaNeeded)
         {
             RETURN_IF_CALLBACK_IERROR(
                 params->decodedCallback->callbackPropertyEnd,
@@ -270,6 +289,10 @@ static int bejProcessEnding(struct BejHandleTypeFuncParam* params,
             // still inside the same section.
             return 0;
         }
+        else
+        {
+            return 0;
+        }
     }
     return 0;
 }
@@ -673,6 +696,46 @@ static int bejHandleBejPropertyAnnotation(struct BejHandleTypeFuncParam* params)
     return 0;
 }
 
+/**
+ * @brief Decodes a BejResourceLink type SFLV BEJ tuple.
+ *
+ * @param[in] params - a valid BejHandleTypeFuncParam struct.
+ * @return 0 if successful.
+ */
+static int bejHandleBejResourceLink(struct BejHandleTypeFuncParam* params)
+{
+    const char* propName = bejGetPropName(params);
+
+    if (params->sflv.valueLength == 0)
+    {
+        RETURN_IF_CALLBACK_IERROR(params->decodedCallback->callbackNull,
+                                  propName, params->callbacksDataPtr);
+    }
+    else
+    {
+        // ResourceLink value is an nnint representing the ResourceID
+        // of Redfish Resource PDR for linked schema
+        uint64_t resourceId = bejGetNnint(params->sflv.value);
+
+        // Check if the callback for ResourceLink exists
+        if (params->decodedCallback->callbackResourceLink != NULL)
+        {
+            RETURN_IF_CALLBACK_IERROR(
+                params->decodedCallback->callbackResourceLink, propName,
+                resourceId, params->callbacksDataPtr);
+        }
+        else
+        {
+            // Fallback: treat as integer if no specific ResourceLink callback
+            RETURN_IF_CALLBACK_IERROR(params->decodedCallback->callbackInteger,
+                                      propName, (int64_t)resourceId,
+                                      params->callbacksDataPtr);
+        }
+    }
+    params->state.encodedStreamOffset = params->sflv.valueEndOffset;
+    return bejProcessEnding(params, /*canBeEmpty=*/false);
+}
+
 /**
  * @brief Decodes an encoded bej stream.
  *
@@ -717,6 +780,7 @@ static int bejDecode(const uint8_t* schemaDictionary,
         .stackCallback = stackCallback,
         .callbacksDataPtr = callbacksDataPtr,
         .stackDataPtr = stackDataPtr,
+        .isCommaNeeded = true,
     };
 
     while (params.state.encodedStreamOffset < streamLen)
@@ -765,24 +829,34 @@ static int bejDecode(const uint8_t* schemaDictionary,
                 // TODO: Add support for BejBytestring decoding.
                 fprintf(stderr, "No BejBytestring support\n");
                 params.state.encodedStreamOffset = params.sflv.valueEndOffset;
+                params.isCommaNeeded = false;
+                RETURN_IF_IERROR(
+                    bejProcessEnding(&params, /*canBeEmpty=*/false));
+                params.isCommaNeeded = true;
                 break;
             case bejChoice:
                 // TODO: Add support for BejChoice decoding.
                 fprintf(stderr, "No BejChoice support\n");
                 params.state.encodedStreamOffset = params.sflv.valueEndOffset;
+                params.isCommaNeeded = false;
+                RETURN_IF_IERROR(
+                    bejProcessEnding(&params, /*canBeEmpty=*/false));
+                params.isCommaNeeded = true;
                 break;
             case bejPropertyAnnotation:
                 RETURN_IF_IERROR(bejHandleBejPropertyAnnotation(&params));
                 break;
             case bejResourceLink:
-                // TODO: Add support for BejResourceLink decoding.
-                fprintf(stderr, "No BejResourceLink support\n");
-                params.state.encodedStreamOffset = params.sflv.valueEndOffset;
+                RETURN_IF_IERROR(bejHandleBejResourceLink(&params));
                 break;
             case bejResourceLinkExpansion:
                 // TODO: Add support for BejResourceLinkExpansion decoding.
                 fprintf(stderr, "No BejResourceLinkExpansion support\n");
                 params.state.encodedStreamOffset = params.sflv.valueEndOffset;
+                params.isCommaNeeded = false;
+                RETURN_IF_IERROR(
+                    bejProcessEnding(&params, /*canBeEmpty=*/false));
+                params.isCommaNeeded = true;
                 break;
             default:
                 break;
diff --git a/src/bej_decoder_json.cpp b/src/bej_decoder_json.cpp
index 5f8ddac..a39ca86 100644
--- a/src/bej_decoder_json.cpp
+++ b/src/bej_decoder_json.cpp
@@ -10,6 +10,7 @@ struct BejJsonParam
 {
     bool* isPrevAnnotated;
     std::string* output;
+    const std::unordered_map<uint32_t, std::string>* resourceIdToUriMap;
 };
 
 /**
@@ -262,6 +263,49 @@ static int callbackAnnotation(const char* propertyName, void* dataPtr)
     return 0;
 }
 
+/**
+ * @brief Callback for bejResourceLink type.
+ *
+ * @param[in] propertyName - a NULL terminated string.
+ * @param[in] resourceId - resource ID value.
+ * @param[in] dataPtr - pointing to a valid BejJsonParam struct.
+ * @return 0 if successful.
+ */
+static int callbackResourceLink(const char* propertyName, uint64_t resourceId,
+                                void* dataPtr)
+{
+    struct BejJsonParam* params =
+        reinterpret_cast<struct BejJsonParam*>(dataPtr);
+    addPropertyNameToOutput(params, propertyName);
+
+    // Try to find URI for this resourceId
+    if (params->resourceIdToUriMap != nullptr)
+    {
+        auto it =
+            params->resourceIdToUriMap->find(static_cast<uint32_t>(resourceId));
+        if (it != params->resourceIdToUriMap->end())
+        {
+            // Found URI mapping, output the URI string
+            params->output->push_back('\"');
+            params->output->append(it->second);
+            params->output->push_back('\"');
+        }
+        else
+        {
+            // No URI mapping found, output as numeric resourceId
+            params->output->append(std::to_string(resourceId));
+        }
+    }
+    else
+    {
+        // No URI mapping provided, output as numeric resourceId
+        params->output->append(std::to_string(resourceId));
+    }
+
+    *params->isPrevAnnotated = false;
+    return 0;
+}
+
 /**
  * @brief Callback for stackEmpty.
  *
@@ -324,8 +368,10 @@ static int stackPush(const struct BejStackProperty* const property,
     return 0;
 }
 
-int BejDecoderJson::decode(const BejDictionaries& dictionaries,
-                           const std::span<const uint8_t> encodedPldmBlock)
+int BejDecoderJson::decode(
+    const BejDictionaries& dictionaries,
+    const std::span<const uint8_t> encodedPldmBlock,
+    const std::unordered_map<uint32_t, std::string>& resourceIdToUriMap)
 {
     // Clear the previous output if any.
     output.clear();
@@ -358,6 +404,7 @@ int BejDecoderJson::decode(const BejDictionaries& dictionaries,
         .callbackReal = callbackReal,
         .callbackBool = callbackBool,
         .callbackAnnotation = callbackAnnotation,
+        .callbackResourceLink = callbackResourceLink,
         .callbackReadonlyProperty = nullptr,
     };
 
@@ -365,6 +412,7 @@ int BejDecoderJson::decode(const BejDictionaries& dictionaries,
     struct BejJsonParam callbackData = {
         .isPrevAnnotated = &isPrevAnnotated,
         .output = &output,
+        .resourceIdToUriMap = &resourceIdToUriMap,
     };
 
     return bejDecodePldmBlock(
-- 
2.34.1

