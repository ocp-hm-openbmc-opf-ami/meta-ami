From 2061788037bde79fb32c848b8d2223359e912e58 Mon Sep 17 00:00:00 2001
From: "vijaysankarr@ami.com" <vijaysankarr@ami.com>
Date: Wed, 13 May 2026 18:26:12 +0530
Subject: [PATCH 285/285] Fix-For-LF-Sync-Build-Issue

---
 lib/buildreq.c     | 2 ++
 lib/sendserver.c   | 1 +
 src/radiusclient.c | 4 ++--
 3 files changed, 5 insertions(+), 2 deletions(-)

diff --git a/lib/buildreq.c b/lib/buildreq.c
index 6664422..a3cf0dd 100644
--- a/lib/buildreq.c
+++ b/lib/buildreq.c
@@ -32,6 +32,8 @@ typedef struct
     size_t tls_buffer_len;
 } eap_tls_state_t;
 
+void eap_tls_cleanup(eap_tls_state_t *state);
+
 /*
  * Function: eap_tls_init_ssl_context
  *
diff --git a/lib/sendserver.c b/lib/sendserver.c
index c1beeb4..60333f7 100644
--- a/lib/sendserver.c
+++ b/lib/sendserver.c
@@ -19,6 +19,7 @@
 #include <includes.h>
 #include <pathnames.h>
 #include <poll.h>
+#include <rc-md5.h>
 
 #if (defined(__APPLE__) || defined(__FreeBSD__)) && defined(HAVE_GETENTROPY)
 #include <sys/random.h>
diff --git a/src/radiusclient.c b/src/radiusclient.c
index 56557dc..a3096e7 100644
--- a/src/radiusclient.c
+++ b/src/radiusclient.c
@@ -75,6 +75,7 @@
 #define EAP_LENGTH_LSB_INDEX 3 // Length (LSB)
 #define EAP_TYPE_INDEX 4       // EAP Type (Identity, TLS, etc.)
 
+extern int rc_auth_eaptls(rc_handle *rh, UINT4 client_port,VALUE_PAIR **received, char *msg);
 bool radiusEnableEapTLSStatus = false;
 struct privilageStruct
 {
@@ -702,8 +703,7 @@ PAM_EXTERN int pam_sm_authenticate(pam_handle_t* pamh, int flags, int argc,
 
     acct = 0;
     server = 0;
-    while ((ch = getopt(argc, argv, "af:p:s")) != -1)
-    {
+    while ((ch = getopt(argc, (char *const *)argv, "af:p:s")) != -1) {
         switch (ch)
         {
             case 'f':
-- 
2.43.0

