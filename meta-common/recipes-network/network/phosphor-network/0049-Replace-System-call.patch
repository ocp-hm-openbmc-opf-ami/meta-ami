From b103e78e4cb6e9e11942dbb3da982d3efe46b47d Mon Sep 17 00:00:00 2001
From: DerickWu <derickwu@ami.com>
Date: Wed, 2 Oct 2024 15:32:48 +0800
Subject: [PATCH] Replace-System-call

---
 src/firewall_configuration.cpp | 76 ++++++++++++++++-----------------
 src/util.cpp                   | 78 ++++++++++++++++++++++++++++++++++
 src/util.hpp                   |  7 +++
 3 files changed, 123 insertions(+), 38 deletions(-)

diff --git a/src/firewall_configuration.cpp b/src/firewall_configuration.cpp
index 36f2958..9fb71e4 100644
--- a/src/firewall_configuration.cpp
+++ b/src/firewall_configuration.cpp
@@ -19,6 +19,8 @@
 
 #include <cstdlib>
 #include <fstream>
+#include <unistd.h>
+#include <iostream>
 
 namespace phosphor
 {
@@ -239,20 +241,20 @@ int16_t Configuration::addRule(FirewallIface::Target target, uint8_t control,
 
     if ((control & (uint8_t)ControlBit::IP) != (uint8_t)ControlBit::IP)
     {
-        ret = std::system(fmt::format("iptables {}", params).c_str());
+        ret = runSystemCommand("iptables", params);
         if (auto index = params.find("icmp"); index != std::string::npos)
             params.replace(index, 4, "icmpv6");
-        ret |= std::system(fmt::format("ip6tables {}", params).c_str());
+        ret |= runSystemCommand("ip6tables", params);
     } // if
     else
     {
         if (startIPAddress.find(":") == std::string::npos)
         {
-            ret = std::system(fmt::format("iptables {}", params).c_str());
+            ret = runSystemCommand("iptables", params);
         } // if
         else
         {
-            ret = std::system(fmt::format("ip6tables {}", params).c_str());
+            ret = runSystemCommand("ip6tables", params);
         }
     } // else
 
@@ -426,18 +428,18 @@ int16_t Configuration::delRule(FirewallIface::Target target, uint8_t control,
 
     if ((control & (uint8_t)ControlBit::IP) != (uint8_t)ControlBit::IP)
     {
-        ret = std::system(fmt::format("iptables {}", params).c_str());
-        ret |= std::system(fmt::format("ip6tables {}", params).c_str());
+        ret = runSystemCommand("iptables", params);
+        ret |= runSystemCommand("ip6tables", params);
     } // if
     else
     {
         if (startIPAddress.find(":") == std::string::npos)
         {
-            ret = std::system(fmt::format("iptables {}", params).c_str());
+            ret = runSystemCommand("iptables", params);
         } // if
         else
         {
-            ret = std::system(fmt::format("ip6tables {}", params).c_str());
+            ret = runSystemCommand("ip6tables", params);
         }
     } // else
 
@@ -603,34 +605,42 @@ std::vector<IPTableElementTuple> Configuration::getRules(FirewallIface::IP ip)
 template <typename T>
 void Configuration::writeConfigurationFile(bool isInit)
 {
+    const char* command;
+    const char* logFilePath;
+    std::string logFilePathStr;
+
     if (typeid(T) == typeid(in6_addr))
     {
         if (isInit)
         {
-            std::system(fmt::format("ip6tables-save | grep Preload > {}/{}",
-                                    AMI_IPTABLES_DIR, IP6TABLES_RULES)
-                            .c_str());
+            command = "ip6tables-save | grep Preload";
+            logFilePathStr = std::string(AMI_IPTABLES_DIR) + "/" + std::string(IP6TABLES_RULES);
+            logFilePath = logFilePathStr.c_str();
+            executeCommandAndLog(command, logFilePath);
         }
         else
         {
-            std::system(fmt::format("ip6tables-save | grep -v Preload > {}/{}",
-                                    CUSTOM_IPTABLES_DIR, IP6TABLES_RULES)
-                            .c_str());
+            command = "ip6tables-save | grep -v Preload";
+            logFilePathStr = std::string(CUSTOM_IPTABLES_DIR) + "/" + std::string(IP6TABLES_RULES);
+            logFilePath = logFilePathStr.c_str();
+            executeCommandAndLog(command, logFilePath);                            
         }
     } // if
     else
     {
         if (isInit)
         {
-            std::system(fmt::format("iptables-save | grep Preload > {}/{}",
-                                    AMI_IPTABLES_DIR, IPTABLES_RULES)
-                            .c_str());
+            command = "iptables-save | grep Preload";
+            logFilePathStr = std::string(AMI_IPTABLES_DIR) + "/" + std::string(IPTABLES_RULES);
+            logFilePath = logFilePathStr.c_str();
+            executeCommandAndLog(command, logFilePath);
         }
         else
         {
-            std::system(fmt::format("iptables-save | grep -v Preload > {}/{}",
-                                    CUSTOM_IPTABLES_DIR, IPTABLES_RULES)
-                            .c_str());
+            command = "iptables-save | grep -v Preload";
+            logFilePathStr = std::string(CUSTOM_IPTABLES_DIR) + "/" + std::string(IPTABLES_RULES);
+            logFilePath = logFilePathStr.c_str();
+            executeCommandAndLog(command, logFilePath);                            
         }
     } // else
 }
@@ -643,18 +653,14 @@ void Configuration::restoreConfigurationFile()
         if (fs::exists(
                 fmt::format("{}/{}", CUSTOM_IPTABLES_DIR, IP6TABLES_RULES)
                     .c_str()))
-            std::system(fmt::format("ip6tables-restore --noflush < {}/{}",
-                                    CUSTOM_IPTABLES_DIR, IP6TABLES_RULES)
-                            .c_str());
+            (void)runSystemCommand("ip6tables-restore", fmt::format("--noflush < {}/{}", CUSTOM_IPTABLES_DIR, IP6TABLES_RULES).c_str());
     } // if
     else
     {
         if (fs::exists(
                 fmt::format("{}/{}", CUSTOM_IPTABLES_DIR, IP6TABLES_RULES)
                     .c_str()))
-            std::system(fmt::format("iptables-restore --noflush < {}/{}",
-                                    CUSTOM_IPTABLES_DIR, IPTABLES_RULES)
-                            .c_str());
+            (void)runSystemCommand("iptables-restore", fmt::format("--noflush < {}/{}", CUSTOM_IPTABLES_DIR, IPTABLES_RULES).c_str());
     } // else
 }
 
@@ -665,26 +671,20 @@ void Configuration::appendPreloadRules()
     if (typeid(T) == typeid(in_addr))
     {
         // Add IPv4 Preload Rules here
-        std::system(
-            "iptables -A INPUT -p icmp --icmp-type 8 -j DROP -m comment --comment \"Preload\"");
+        (void)runSystemCommand("iptables", "-A INPUT -p icmp --icmp-type 8 -j DROP -m comment --comment \"Preload\"");
 
-        std::system(
-            "iptables -A INPUT -p icmp --icmp-type 13 -j DROP -m comment --comment \"Preload\"");
+        (void)runSystemCommand("iptables", "-A INPUT -p icmp --icmp-type 13 -j DROP -m comment --comment \"Preload\"");
 
-        std::system(
-            "iptables -A INPUT -p icmp --icmp-type 14 -j DROP -m comment --comment \"Preload\"");
+        (void)runSystemCommand("iptables", "-A INPUT -p icmp --icmp-type 14 -j DROP -m comment --comment \"Preload\"");
 
-        std::system(
-            "iptables -A INPUT -p icmp --icmp-type 11 -j DROP -m comment --comment \"Preload\"");
+        (void)runSystemCommand("iptables", "-A INPUT -p icmp --icmp-type 11 -j DROP -m comment --comment \"Preload\"");
 
-        std::system(
-            "iptables -A OUTPUT -p icmp --icmp-type 11 -j DROP -m comment --comment \"Preload\"");
+        (void)runSystemCommand("iptables", "-A OUTPUT -p icmp --icmp-type 11 -j DROP -m comment --comment \"Preload\"");
     } // if
     else
     {
         // Add IPv6 Preload Rules here
-        std::system(
-            "ip6tables -A INPUT -p icmpv6 --icmpv6-type 128 -j DROP -m comment --comment \"Preload\"");
+        (void)runSystemCommand("ip6tables", "-A INPUT -p icmpv6 --icmpv6-type 128 -j DROP -m comment --comment \"Preload\"");
     } // else
 #endif
 }
diff --git a/src/util.cpp b/src/util.cpp
index 1a34fca..4e0e50f 100644
--- a/src/util.cpp
+++ b/src/util.cpp
@@ -18,6 +18,15 @@
 #include <cctype>
 #include <string>
 #include <string_view>
+#include <unistd.h>
+#include <iostream>
+#include <cstdio>
+#include <memory>
+#include <stdexcept>
+#include <array>
+#include <sstream>
+#include <vector>
+
 
 namespace phosphor
 {
@@ -125,6 +134,75 @@ const std::unordered_set<std::string_view>& getIgnoredInterfaces()
 
 } // namespace internal
 
+int runSystemCommand(const char* cmd, const std::string& params) 
+{
+    std::istringstream iss(params);
+    std::vector<std::string> paramList;
+    std::string param;
+
+    while (iss >> param) {
+        paramList.push_back(param);
+    }
+
+    std::vector<const char*> args;
+    args.push_back(cmd);
+    for (const auto& p : paramList) {
+        args.push_back(p.c_str());
+    }
+    args.push_back(nullptr);
+
+    pid_t pid = fork();
+    if (pid == 0) 
+    {
+        execvp(cmd, args.data());
+
+        perror("execvp failed");
+        _exit(1);
+    } 
+    else if (pid < 0) 
+    {
+        perror("fork failed");
+        return -1;
+    }
+
+    int status;
+    waitpid(pid, &status, 0);
+    return status;
+}
+
+std::string runCommandAndStoreLog(const char* cmd) {
+    std::array<char, 128> buffer;
+    std::string result;
+
+    std::unique_ptr<FILE, decltype(&pclose)> pipe(popen(cmd, "r"), pclose);
+    if (!pipe) {
+        throw std::runtime_error("popen() failed!");
+    }
+
+    while (fgets(buffer.data(), buffer.size(), pipe.get()) != nullptr) {
+        result += buffer.data();
+    }
+
+    return result;
+}
+
+void executeCommandAndLog(const char* command, const char* logFilePath) {
+    try {
+        std::string output = runCommandAndStoreLog(command);
+
+        FILE* logFile = fopen(logFilePath, "w");
+        if (logFile) {
+            fputs(output.c_str(), logFile);
+            fclose(logFile);
+            std::cout << "Log written to " << logFilePath << std::endl;
+        } else {
+            std::cerr << "Failed to open log file: " << logFilePath << std::endl;
+        }
+    } catch (const std::exception& e) {
+        std::cerr << "Error: " << e.what() << std::endl;
+    }
+}
+
 std::optional<std::string> interfaceToUbootEthAddr(std::string_view intf)
 {
     constexpr auto pfx = "eth"sv;
diff --git a/src/util.hpp b/src/util.hpp
index 9f4e374..dda96a1 100644
--- a/src/util.hpp
+++ b/src/util.hpp
@@ -218,6 +218,13 @@ void execute(stdplus::zstring_view path, ArgTypes&&... tArgs)
  */
 std::vector<std::string> splitStr(std::string line, std::string delimiter);
 
+/** @brief Use exec instead of system call to run command */
+int runSystemCommand(const char* cmd, const std::string& params);
+
+std::string runCommandAndStoreLog(const char* cmd);
+
+void executeCommandAndLog(const char* command, const char* logFilePath);
+
 } // namespace network
 
 } // namespace phosphor
-- 
2.25.1

