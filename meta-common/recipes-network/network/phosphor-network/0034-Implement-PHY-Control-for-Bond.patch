From 7a140f4c241250bb1008e31be50a5a9478986608 Mon Sep 17 00:00:00 2001
From: Ethan Wei <ethanwei@ami.com>
Date: Wed, 17 Apr 2024 16:16:45 +0800
Subject: [PATCH] Implement PHY Control for Bond

---
 src/bond.cpp               | 32 ++++++++++++++++++++++----------
 src/bond.hpp               |  7 ++++++-
 src/ethernet_interface.cpp | 15 +++++++++------
 3 files changed, 37 insertions(+), 17 deletions(-)

diff --git a/src/bond.cpp b/src/bond.cpp
index 2f5827b..c419cda 100644
--- a/src/bond.cpp
+++ b/src/bond.cpp
@@ -4,12 +4,13 @@
 #include "network_manager.hpp"
 #include "system_queries.hpp"
 
+#include <nlohmann/json.hpp>
 #include <phosphor-logging/elog-errors.hpp>
 #include <phosphor-logging/elog.hpp>
 #include <phosphor-logging/lg2.hpp>
 #include <phosphor-logging/log.hpp>
 #include <xyz/openbmc_project/Common/error.hpp>
-#include <nlohmann/json.hpp>
+
 #include <algorithm>
 #include <cstdlib>
 #include <fstream>
@@ -78,7 +79,8 @@ void Bond::delete_()
     /**Save Information of Bond0 and later restore*/
     auto it = eth.manager.get().interfaces.find(intf);
     std::map<std::string, std::variant<bool, std::string,
-                                       std::vector<std::optional<std::string>>>>
+                                       std::vector<std::optional<std::string>>,
+                                       uint8_t, uint32_t>>
         map;
     if (it != eth.manager.get().interfaces.end())
     {
@@ -88,6 +90,9 @@ void Bond::delete_()
         map.emplace("ipv6IndexUsedList", it->second->ipv6IndexUsedList);
         map.emplace("ipv4Enable", it->second->ipv4Enable());
         map.emplace("ipv6Enable", it->second->ipv6Enable());
+        map.emplace("autoNeg", it->second->autoNeg());
+        map.emplace("duplex", static_cast<uint8_t>(it->second->duplex()));
+        map.emplace("speed", it->second->speed());
     }
     auto obj = std::move(it->second);
     eth.manager.get().interfaces.erase(it);
@@ -169,7 +174,8 @@ Bond::Mode Bond::mode(Mode /*Bonding Mode*/)
 
 void Bond::restoreConfiguration(
     std::map<std::string, std::variant<bool, std::string,
-                                       std::vector<std::optional<std::string>>>>
+                                       std::vector<std::optional<std::string>>,
+                                       uint8_t, uint32_t>>
         map)
 {
     if (auto it = eth.manager.get().interfaces.find(activeSlave());
@@ -190,6 +196,15 @@ void Bond::restoreConfiguration(
                 std::get<bool>(map["ipv6EnableStaticRtr"]), true);
             it->second->EthernetInterfaceIntf::ipv6StaticRtrAddr(
                 std::get<std::string>(map["ipv6StaticRtrAddr"]), true);
+            it->second->EthernetInterfaceIntf::autoNeg(
+                std::get<bool>(map["autoNeg"]), true);
+            it->second->EthernetInterfaceIntf::duplex(
+                (std::get<uint8_t>(map["duplex"]) == 1
+                     ? EthernetInterface::Duplex::full
+                     : EthernetInterface::Duplex::half),
+                true);
+            it->second->EthernetInterfaceIntf::speed(
+                std::get<uint32_t>(map["speed"]), true);
         }
         {
             config::Parser config(config::pathForIntfConf(
@@ -229,7 +244,6 @@ void Bond::writeBondConfiguration(bool isActive)
     std::ofstream ofs;
     std::ifstream ifs;
     std::string intfName, line, IfaceConfDir;
-    std::filebuf infile, outfile;
     if (isActive)
     {
         ifs.open(config::pathForIntfConf(
@@ -249,12 +263,6 @@ void Bond::writeBondConfiguration(bool isActive)
         }
 
         intfName = "Name=bond0";
-        infile.open(config::pathForIntfInfo(eth.manager.get().getIfaceConfDir(),
-                                            BondIntf::activeSlave()),
-                    std::ios::in | std::ios::binary);
-        outfile.open(config::pathForIntfInfo(
-                         eth.manager.get().getIfaceConfDir(), "bond0"),
-                     std::ios::out | std::ios::binary);
     } // if
     else
     {
@@ -275,13 +283,6 @@ void Bond::writeBondConfiguration(bool isActive)
         }
 
         intfName = fmt::format("Name={}", BondIntf::activeSlave());
-        infile.open(config::pathForIntfInfo(eth.manager.get().getIfaceConfDir(),
-                                            "bond0"),
-                    std::ios::in | std::ios::binary);
-        outfile.open(
-            config::pathForIntfInfo(eth.manager.get().getIfaceConfDir(),
-                                    BondIntf::activeSlave()),
-            std::ios::out | std::ios::binary);
     } // else
 
     while (ifs.peek() != EOF)
@@ -301,10 +302,6 @@ void Bond::writeBondConfiguration(bool isActive)
     ofs.flush();
     ofs.close();
     ifs.close();
-    std::copy(std::istreambuf_iterator<char>(&infile), {},
-              std::ostreambuf_iterator<char>(&outfile));
-    infile.close();
-    outfile.close();
 
     auto readJsonFile =
         [](const std::string& configFile) -> nlohmann::ordered_json {
diff --git a/src/bond.hpp b/src/bond.hpp
index c5e7962..c3bea39 100644
--- a/src/bond.hpp
+++ b/src/bond.hpp
@@ -67,7 +67,8 @@ class Bond : public BondObj
     void restoreConfiguration(
         std::map<std::string,
                  std::variant<bool, std::string,
-                              std::vector<std::optional<std::string>>>>
+                              std::vector<std::optional<std::string>>, uint8_t,
+                              uint32_t>>
             map);
 
     inline const auto& getObjPath() const
diff --git a/src/ethernet_interface.cpp b/src/ethernet_interface.cpp
index 74282f5..4b9c4ce 100644
--- a/src/ethernet_interface.cpp
+++ b/src/ethernet_interface.cpp
@@ -190,7 +190,7 @@ EthernetInterface::EthernetInterface(stdplus::PinnedRef<sdbusplus::bus_t> bus,
         try {
             if (auto phyConf = getPHYInfo(ifaceConfig); phyConf.has_value())
             {
-                if (!this->vlan.has_value())
+                if (!this->vlan.has_value() && !this->bonding.has_value())
                 {
                     auto [autoNeg, duplex, speed] = phyConf.value();
                     if (!autoNeg && !duplex.empty() || speed > 0)
@@ -1465,7 +1466,6 @@ ObjectPath EthernetInterface::createBond(std::string activeSlave,
             ipv6EnableStaticRtr, true);
         it->second->EthernetInterfaceIntf::ipv6StaticRtrAddr(ipv6StaticRtrAddr,
                                                              true);
-        it->second->bonding->writeBondConfiguration(true);
     }
 
     execute("/bin/systemctl", "systemctl", "restart",
@@ -1767,6 +1767,9 @@ void EthernetInterface::writeConfigurationFile()
                 network["PrimarySlave"].emplace_back("true");
             }
         }
+        {
+            writeIfaceStateFile(interfaceName());
+        }
     }
     else
     {
-- 
2.43.0

