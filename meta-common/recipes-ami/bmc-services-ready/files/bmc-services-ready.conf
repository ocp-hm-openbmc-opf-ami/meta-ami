INTERVAL=5
RETRY_COUNT=6
SERVICES=("phosphor-ipmi-net@eth0.service" "bmcweb.service" "xyz.openbmc_project.Software.BMC.Updater.service" "xyz.openbmc_project.Software.Version.service")

# BMC UUID D-Bus interface (xyz.openbmc_project.Common.UUID) readiness poll.
# UUID_POLL_INTERVAL: how often to poll for the interface (seconds).
# UUID_TIMEOUT_SEC:   how long to wait for it before giving up (seconds).
UUID_POLL_INTERVAL="0.2"
UUID_TIMEOUT_SEC=3
