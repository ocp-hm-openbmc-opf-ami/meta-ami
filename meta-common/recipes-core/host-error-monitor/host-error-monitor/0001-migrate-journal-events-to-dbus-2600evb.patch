From b4fd551fac41fe8717799cdaeac575f148720bdb Mon Sep 17 00:00:00 2001
From: Minal Upadhye <minalsunilu@ami.com>
Date: Mon, 21 Jul 2025 02:52:19 -0400
Subject: [PATCH] migrate-journal-events-to-dbus-2600evb

Signed-off-by: Minal Upadhye <minalsunilu@ami.com>
---
 include/error_monitors/cpld_crc_monitor.hpp   | 35 ++++++---
 .../cpu_early_error_monitor.hpp               | 29 +++++--
 .../error_monitors/cpu_mismatch_monitor.hpp   | 36 ++++++---
 .../error_monitors/cpu_presence_monitor.hpp   | 35 ++++++---
 .../error_monitors/cpu_thermtrip_monitor.hpp  | 31 ++++++--
 include/error_monitors/err_pin_monitor.hpp    | 55 ++++++++++---
 .../err_pin_timeout_monitor.hpp               | 52 +++++++++++--
 include/error_monitors/ierr_monitor.hpp       | 78 ++++++++++++++++---
 include/error_monitors/mcerr_monitor.hpp      | 29 +++++--
 .../error_monitors/mem_thermtrip_monitor.hpp  | 31 ++++++--
 include/error_monitors/memhot_monitor.hpp     | 31 ++++++--
 .../error_monitors/pch_thermtrip_monitor.hpp  | 23 +++++-
 include/error_monitors/prochot_monitor.hpp    | 31 ++++++--
 include/error_monitors/smi_monitor.hpp        | 27 ++++++-
 include/error_monitors/vr_hot_monitor.hpp     | 31 ++++++--
 15 files changed, 449 insertions(+), 105 deletions(-)

diff --git a/include/error_monitors/cpld_crc_monitor.hpp b/include/error_monitors/cpld_crc_monitor.hpp
index cd5eb24..bcf1641 100644
--- a/include/error_monitors/cpld_crc_monitor.hpp
+++ b/include/error_monitors/cpld_crc_monitor.hpp
@@ -36,10 +36,28 @@ class CPLDCRCMonitor :
         std::string cpuNumber = "CPU " + std::to_string(cpuNum);
         std::string msg = cpuNumber + " CPLD CRC error.";
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     bool getCPUPresence(const std::string& cpuPresenceName)
@@ -55,9 +73,9 @@ class CPLDCRCMonitor :
         // Request GPIO input
         try
         {
-            cpuPresenceLine.request({"host-error-monitor",
-                                     gpiod::line_request::DIRECTION_INPUT,
-                                     gpiod::line_request::FLAG_ACTIVE_LOW});
+            cpuPresenceLine.request(
+                {"host-error-monitor", gpiod::line_request::DIRECTION_INPUT,
+                 gpiod::line_request::FLAG_ACTIVE_LOW});
         }
         catch (std::exception&)
         {
@@ -93,8 +111,7 @@ class CPLDCRCMonitor :
                    std::shared_ptr<sdbusplus::asio::connection> conn,
                    const std::string& signalName, const size_t cpuNum,
                    const std::string& cpuPresenceName) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        cpuNum(cpuNum)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), cpuNum(cpuNum)
     {
         if (!getCPUPresence(cpuPresenceName))
         {
diff --git a/include/error_monitors/cpu_early_error_monitor.hpp b/include/error_monitors/cpu_early_error_monitor.hpp
index 5e048b2..3e3d17d 100644
--- a/include/error_monitors/cpu_early_error_monitor.hpp
+++ b/include/error_monitors/cpu_early_error_monitor.hpp
@@ -33,18 +33,35 @@ class CPUEarlyErrorMonitor :
         std::string cpuNumber = "CPU " + std::to_string(cpuNum);
         std::string msg = cpuNumber + " early error.";
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_ERR, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
   public:
     CPUEarlyErrorMonitor(boost::asio::io_context& io,
                          std::shared_ptr<sdbusplus::asio::connection> conn,
                          const std::string& signalName, const size_t cpuNum) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        cpuNum(cpuNum)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), cpuNum(cpuNum)
     {
         if (valid)
         {
diff --git a/include/error_monitors/cpu_mismatch_monitor.hpp b/include/error_monitors/cpu_mismatch_monitor.hpp
index 2d65767..03a4c4e 100644
--- a/include/error_monitors/cpu_mismatch_monitor.hpp
+++ b/include/error_monitors/cpu_mismatch_monitor.hpp
@@ -34,10 +34,29 @@ class CPUMismatchMonitor : public host_error_monitor::base_monitor::BaseMonitor
 
     void cpuMismatchLog()
     {
-        sd_journal_send("MESSAGE=HostError: CPU %d mismatch", cpuNum,
-                        "PRIORITY=%i", LOG_ERR, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUMismatch", "REDFISH_MESSAGE_ARGS=%d",
-                        cpuNum, NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUMismatch";
+        std::string arg = std::to_string(cpuNum);
+        std::string journalMsg = messageID + "," + arg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = arg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     bool requestCPUMismatchInput()
@@ -53,9 +72,9 @@ class CPUMismatchMonitor : public host_error_monitor::base_monitor::BaseMonitor
         // Request GPIO input
         try
         {
-            cpuMismatchLine.request({"host-error-monitor",
-                                     gpiod::line_request::DIRECTION_INPUT,
-                                     0}); // 0 indicates ACTIVE_HIGH
+            cpuMismatchLine.request(
+                {"host-error-monitor", gpiod::line_request::DIRECTION_INPUT,
+                 0}); // 0 indicates ACTIVE_HIGH
         }
         catch (std::exception&)
         {
@@ -94,8 +113,7 @@ class CPUMismatchMonitor : public host_error_monitor::base_monitor::BaseMonitor
     CPUMismatchMonitor(boost::asio::io_context& io,
                        std::shared_ptr<sdbusplus::asio::connection> conn,
                        const std::string& signalName, const size_t cpuNum) :
-        BaseMonitor(io, conn, signalName),
-        cpuNum(cpuNum)
+        BaseMonitor(io, conn, signalName), cpuNum(cpuNum)
     {
         // Request GPIO input
         if (!requestCPUMismatchInput())
diff --git a/include/error_monitors/cpu_presence_monitor.hpp b/include/error_monitors/cpu_presence_monitor.hpp
index 2866d48..a737903 100644
--- a/include/error_monitors/cpu_presence_monitor.hpp
+++ b/include/error_monitors/cpu_presence_monitor.hpp
@@ -34,10 +34,28 @@ class CPUPresenceMonitor : public host_error_monitor::base_monitor::BaseMonitor
     {
         std::string msg = "CPU " + std::to_string(cpuNum) + " missing";
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void CPUPresenceAssertHandler(
@@ -62,9 +80,9 @@ class CPUPresenceMonitor : public host_error_monitor::base_monitor::BaseMonitor
         // Request GPIO input
         try
         {
-            cpuPresenceLine.request({"host-error-monitor",
-                                     gpiod::line_request::DIRECTION_INPUT,
-                                     gpiod::line_request::FLAG_ACTIVE_LOW});
+            cpuPresenceLine.request(
+                {"host-error-monitor", gpiod::line_request::DIRECTION_INPUT,
+                 gpiod::line_request::FLAG_ACTIVE_LOW});
         }
         catch (const std::exception&)
         {
@@ -90,8 +108,7 @@ class CPUPresenceMonitor : public host_error_monitor::base_monitor::BaseMonitor
     CPUPresenceMonitor(boost::asio::io_context& io,
                        std::shared_ptr<sdbusplus::asio::connection> conn,
                        const std::string& signalName, const size_t cpuNum) :
-        BaseMonitor(io, conn, signalName),
-        cpuNum(cpuNum)
+        BaseMonitor(io, conn, signalName), cpuNum(cpuNum)
     {
         if (!getCPUPresence(signalName))
         {
diff --git a/include/error_monitors/cpu_thermtrip_monitor.hpp b/include/error_monitors/cpu_thermtrip_monitor.hpp
index f1b7758..eba0b6f 100644
--- a/include/error_monitors/cpu_thermtrip_monitor.hpp
+++ b/include/error_monitors/cpu_thermtrip_monitor.hpp
@@ -39,10 +39,30 @@ class CPUThermtripMonitor :
     {
         std::string msg = "CPU " + std::to_string(cpuNum) + " thermal trip";
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUThermalTrip", "REDFISH_MESSAGE_ARGS=%d",
-                        cpuNum, NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUThermalTrip";
+        std::string arg = std::to_string(cpuNum);
+        std::string journalMsg = messageID + "," + arg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = arg;
+        additionalData["Additional_Data"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void deassertHandler() override
@@ -55,8 +75,7 @@ class CPUThermtripMonitor :
                         std::shared_ptr<sdbusplus::asio::connection> conn,
                         const std::string& signalName, const size_t cpuNum,
                         const std::string& customName = std::string()) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        cpuNum(cpuNum)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), cpuNum(cpuNum)
     {
         sdbusplus::asio::object_server server =
             sdbusplus::asio::object_server(conn);
diff --git a/include/error_monitors/err_pin_monitor.hpp b/include/error_monitors/err_pin_monitor.hpp
index 04e3654..f5e3153 100644
--- a/include/error_monitors/err_pin_monitor.hpp
+++ b/include/error_monitors/err_pin_monitor.hpp
@@ -56,10 +56,28 @@ class ErrPinMonitor :
     {
         std::string msg = "ERR" + std::to_string(errPin);
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void errPinLog(const int cpuNum)
@@ -67,18 +85,35 @@ class ErrPinMonitor :
         std::string msg = "ERR" + std::to_string(errPin) + " on CPU " +
                           std::to_string(cpuNum + 1);
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
   public:
     ErrPinMonitor(boost::asio::io_context& io,
                   std::shared_ptr<sdbusplus::asio::connection> conn,
                   const std::string& signalName, const size_t errPin) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        errPin(errPin)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), errPin(errPin)
     {
         if (valid)
         {
diff --git a/include/error_monitors/err_pin_timeout_monitor.hpp b/include/error_monitors/err_pin_timeout_monitor.hpp
index 5afdb10..ac0bf70 100644
--- a/include/error_monitors/err_pin_timeout_monitor.hpp
+++ b/include/error_monitors/err_pin_timeout_monitor.hpp
@@ -57,10 +57,28 @@ class ErrPinTimeoutMonitor :
     {
         std::string msg = "ERR" + std::to_string(errPin) + " Timeout";
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void errPinTimeoutLog(const int cpuNum)
@@ -68,10 +86,28 @@ class ErrPinTimeoutMonitor :
         std::string msg = "ERR" + std::to_string(errPin) + " Timeout on CPU " +
                           std::to_string(cpuNum + 1);
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void startPolling() override
diff --git a/include/error_monitors/ierr_monitor.hpp b/include/error_monitors/ierr_monitor.hpp
index ebd5f20..18eab57 100644
--- a/include/error_monitors/ierr_monitor.hpp
+++ b/include/error_monitors/ierr_monitor.hpp
@@ -54,29 +54,85 @@ class IERRMonitor :
 
     void cpuIERRLog()
     {
-        sd_journal_send("MESSAGE=HostError: IERR", "PRIORITY=%i", LOG_INFO,
-                        "REDFISH_MESSAGE_ID=%s", "OpenBMC.0.1.CPUError",
-                        "REDFISH_MESSAGE_ARGS=%s", "IERR", NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string arg = "IERR";
+        std::string journalMsg = messageID + "," + arg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = arg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void cpuIERRLog(const int cpuNum)
     {
         std::string msg = "IERR on CPU " + std::to_string(cpuNum + 1);
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void cpuIERRLog(const int cpuNum, const std::string& type)
     {
         std::string msg = type + " IERR on CPU " + std::to_string(cpuNum + 1);
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     bool checkIERRCPUs()
diff --git a/include/error_monitors/mcerr_monitor.hpp b/include/error_monitors/mcerr_monitor.hpp
index 8a1f9e6..9b18879 100644
--- a/include/error_monitors/mcerr_monitor.hpp
+++ b/include/error_monitors/mcerr_monitor.hpp
@@ -29,10 +29,28 @@ class MCERRMonitor :
     {
         std::string msg = "MCERR on CPU " + std::to_string(cpuNum);
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        msg.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string journalMsg = messageID + "," + msg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
   public:
@@ -42,8 +60,7 @@ class MCERRMonitor :
         const std::string& signalName,
         const host_error_monitor::base_gpio_monitor::AssertValue assertValue,
         const size_t cpuNum) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        cpuNum(cpuNum)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), cpuNum(cpuNum)
     {
         if (valid)
         {
diff --git a/include/error_monitors/mem_thermtrip_monitor.hpp b/include/error_monitors/mem_thermtrip_monitor.hpp
index ee46463..08061fd 100644
--- a/include/error_monitors/mem_thermtrip_monitor.hpp
+++ b/include/error_monitors/mem_thermtrip_monitor.hpp
@@ -34,10 +34,30 @@ class MemThermtripMonitor :
         std::string cpuNumber = "CPU " + std::to_string(cpuNum);
         std::string msg = cpuNumber + " Memory Thermal trip.";
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_ERR, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.MemoryThermTrip",
-                        "REDFISH_MESSAGE_ARGS=%s", cpuNumber.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.MemoryThermTrip";
+        std::string arg = cpuNumber.c_str();
+        std::string journalMsg = messageID + "," + arg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = arg;
+        additionalData["Additional_Data"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
         assertInterface->set_property("Asserted", true);
     }
 
@@ -51,8 +71,7 @@ class MemThermtripMonitor :
                         std::shared_ptr<sdbusplus::asio::connection> conn,
                         const std::string& signalName, const size_t cpuNum,
                         const std::string& customName = std::string()) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        cpuNum(cpuNum)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), cpuNum(cpuNum)
     {
         sdbusplus::asio::object_server server =
             sdbusplus::asio::object_server(conn);
diff --git a/include/error_monitors/memhot_monitor.hpp b/include/error_monitors/memhot_monitor.hpp
index 90beaaa..ca4b8ba 100644
--- a/include/error_monitors/memhot_monitor.hpp
+++ b/include/error_monitors/memhot_monitor.hpp
@@ -34,18 +34,37 @@ class MemhotMonitor :
         std::string msg = cpuNumber + " Memhot.";
         std::string redfishMsgArgs = cpuNumber + " memory";
 
-        sd_journal_send(
-            "MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i", LOG_ERR,
-            "REDFISH_MESSAGE_ID=%s", "OpenBMC.0.1.ComponentOverTemperature",
-            "REDFISH_MESSAGE_ARGS=%s", redfishMsgArgs.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.ComponentOverTemperature";
+        std::string arg = redfishMsgArgs.c_str();
+        std::string journalMsg = messageID + "," + arg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = arg;
+        additionalData["Additional_Data"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
   public:
     MemhotMonitor(boost::asio::io_context& io,
                   std::shared_ptr<sdbusplus::asio::connection> conn,
                   const std::string& signalName, const size_t cpuNum) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        cpuNum(cpuNum)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), cpuNum(cpuNum)
     {
         if (valid)
         {
diff --git a/include/error_monitors/pch_thermtrip_monitor.hpp b/include/error_monitors/pch_thermtrip_monitor.hpp
index a5b395d..ac29bb1 100644
--- a/include/error_monitors/pch_thermtrip_monitor.hpp
+++ b/include/error_monitors/pch_thermtrip_monitor.hpp
@@ -33,9 +33,26 @@ class PCHThermtripMonitor :
 
     void logEvent() override
     {
-        sd_journal_send("MESSAGE=HostError: SSB thermal trip", "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.SsbThermalTrip", NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string journalMsg = "OpenBMC.0.1.SsbThermalTrip";
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = "OpenBMC.0.1.SsbThermalTrip";
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void assertHandler() override
diff --git a/include/error_monitors/prochot_monitor.hpp b/include/error_monitors/prochot_monitor.hpp
index 9ea6273..07be42e 100644
--- a/include/error_monitors/prochot_monitor.hpp
+++ b/include/error_monitors/prochot_monitor.hpp
@@ -34,18 +34,37 @@ class ProchotMonitor :
         std::string msg = cpuNumber + " Prochot.";
         std::string redfishMsgArgs = cpuNumber;
 
-        sd_journal_send(
-            "MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i", LOG_ERR,
-            "REDFISH_MESSAGE_ID=%s", "OpenBMC.0.1.ComponentOverTemperature",
-            "REDFISH_MESSAGE_ARGS=%s", redfishMsgArgs.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.ComponentOverTemperature";
+        std::string arg = redfishMsgArgs.c_str();
+        std::string journalMsg = messageID + "," + arg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = arg;
+        additionalData["Additional_Data"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
   public:
     ProchotMonitor(boost::asio::io_context& io,
                    std::shared_ptr<sdbusplus::asio::connection> conn,
                    const std::string& signalName, const size_t cpuNum) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        cpuNum(cpuNum)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), cpuNum(cpuNum)
     {
         if (valid)
         {
diff --git a/include/error_monitors/smi_monitor.hpp b/include/error_monitors/smi_monitor.hpp
index 921f95d..a1bea9c 100644
--- a/include/error_monitors/smi_monitor.hpp
+++ b/include/error_monitors/smi_monitor.hpp
@@ -37,10 +37,29 @@ class SMIMonitor :
 
     void logEvent() override
     {
-        sd_journal_send("MESSAGE=HostError: SMI Timeout", "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.CPUError", "REDFISH_MESSAGE_ARGS=%s",
-                        "SMI Timeout", NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.CPUError";
+        std::string arg = "SMI Timeout";
+        std::string journalMsg = messageID + "," + arg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = arg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
     void assertHandler() override
diff --git a/include/error_monitors/vr_hot_monitor.hpp b/include/error_monitors/vr_hot_monitor.hpp
index fe8aa39..611415a 100644
--- a/include/error_monitors/vr_hot_monitor.hpp
+++ b/include/error_monitors/vr_hot_monitor.hpp
@@ -32,18 +32,37 @@ class VRHotMonitor :
     {
         std::string msg = vrName + " Voltage Regulator Overheated.";
 
-        sd_journal_send("MESSAGE=HostError: %s", msg.c_str(), "PRIORITY=%i",
-                        LOG_INFO, "REDFISH_MESSAGE_ID=%s",
-                        "OpenBMC.0.1.VoltageRegulatorOverheated",
-                        "REDFISH_MESSAGE_ARGS=%s", vrName.c_str(), NULL);
+        std::string severity =
+            "xyz.openbmc_project.Logging.Entry.Level.Critical";
+        auto bus = sdbusplus::bus::new_default_system();
+        sdbusplus::message::message m = bus.new_method_call(
+            "xyz.openbmc_project.Logging", "/xyz/openbmc_project/logging",
+            "xyz.openbmc_project.Logging.Create", "Create");
+        std::string messageID = "OpenBMC.0.1.VoltageRegulatorOverheated";
+        std::string arg = vrName.c_str();
+        std::string journalMsg = messageID + "," + arg;
+
+        std::map<std::string, std::string> additionalData;
+        additionalData["REDFISH_MESSAGE_ID"] = messageID;
+        additionalData["REDFISH_MESSAGE_ARGS"] = arg;
+        additionalData["Additional_Data"] = msg;
+        m.append(journalMsg, severity, additionalData);
+        try
+        {
+            bus.call(m);
+        }
+        catch (const sdbusplus::exception_t& e)
+        {
+            lg2::error("Failed to create log entry: {ERROR}", "ERROR",
+                       e.what());
+        }
     }
 
   public:
     VRHotMonitor(boost::asio::io_context& io,
                  std::shared_ptr<sdbusplus::asio::connection> conn,
                  const std::string& signalName, const std::string& vrName) :
-        BaseGPIOMonitor(io, conn, signalName, assertValue),
-        vrName(vrName)
+        BaseGPIOMonitor(io, conn, signalName, assertValue), vrName(vrName)
     {
         if (valid)
         {
-- 
2.34.1

