CONFIG_ASPEED_LTPI_GPIO=y
CONFIG_BOOTCOMMAND="ltpi;echo Boot from ${boot_device}; if test ${boot_device} = mmc; then run bootmmc; fi; if test ${boot_device} = spi; then run bootspi; fi; if test ${boot_device} = ufs; then run bootufs; fi;"
CONFIG_MISC_INIT_R=y
